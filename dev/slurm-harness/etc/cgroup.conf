# Containers have no systemd and no dbus. From 24.x slurmd accepts an explicit opt-out, which is
# the only clean way to stop it creating a slurmstepd systemd scope at startup. On 23.11 and older
# this value does not exist and slurmd will refuse to start.
CgroupPlugin=disabled
