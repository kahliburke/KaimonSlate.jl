import{_ as a,o as t,c as o,a2 as n}from"./chunks/framework.B08aFN1B.js";const p=JSON.parse('{"title":"The Dependency Graph","description":"","frontmatter":{},"headers":[],"relativePath":"dag.md","filePath":"dag.md","lastUpdated":null}'),s={name:"dag.md"};function r(l,e,i,d,h,c){return t(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="The-Dependency-Graph" tabindex="-1">The Dependency Graph <a class="header-anchor" href="#The-Dependency-Graph" aria-label="Permalink to &quot;The Dependency Graph {#The-Dependency-Graph}&quot;">​</a></h1><p>Every notebook is a <strong>directed acyclic graph</strong> of cells: the engine reads which variables each cell reads and writes, and draws an edge wherever one cell consumes another&#39;s value. That graph is what makes the notebook <a href="./reactivity">reactive</a> — change a value and only the downstream cells restale.</p><p>This page is about <strong>seeing and shaping</strong> that graph: the DAG pane, manual edges for effects no variable carries, and running part of a notebook on another kernel (regions).</p><h2 id="The-DAG-pane" tabindex="-1">The DAG pane <a class="header-anchor" href="#The-DAG-pane" aria-label="Permalink to &quot;The DAG pane {#The-DAG-pane}&quot;">​</a></h2><p>Open the pane with the <strong>🕸 DAG</strong> button in the top bar, or <strong>⌘⇧G</strong>. It&#39;s a live map of the notebook&#39;s dataflow — one node per cell, an edge wherever a cell reads a value another cell writes — laid out automatically and updated as you edit and run.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/dag-pane.png" alt=""></p><ul><li><p><strong>Node color</strong> mirrors each cell&#39;s <a href="/KaimonSlate.jl/dev/reactivity#cell-states">state</a> — fresh / stale / running / errored — so a wave of gold shows exactly what a change restaled.</p></li><li><p><strong>⚙ display</strong> — <strong>show setup cells</strong> (<code>using</code>/<code>import</code> and their edges) and <strong>show isolated cells</strong> (cells with no dataflow edges) are off by default, keeping the graph to the cells that actually pass data.</p></li><li><p><strong>⇅ direction</strong> — layout orientation; <em>auto</em> follows the pane shape (a tall pane lays out top-down).</p></li><li><p><strong>◨ dock</strong> and the <strong>grip</strong> re-side and resize the pane.</p></li><li><p><strong>🔥 heat map</strong> — color cells by accumulated compute time (hotter = more expensive), to find the bottleneck in a pipeline.</p></li><li><p><strong>🖧 region map</strong> — color cells by <em>where they run</em> (see <a href="/KaimonSlate.jl/dev/dag#regions">Regions</a>).</p></li></ul><h2 id="Manual-edges" tabindex="-1">Manual edges <a class="header-anchor" href="#Manual-edges" aria-label="Permalink to &quot;Manual edges {#Manual-edges}&quot;">​</a></h2><p>The engine infers edges from the variables a cell reads and writes. Some dependencies aren&#39;t carried by any variable, though — a cell that writes a file, seeds a database table, or mutates global state that a later cell reads back. Assert those yourself so the engine keeps the two in order:</p><ul><li><p><strong><code>needs=id1,id2</code></strong> header tag — set it with the <strong>🏷 tag editor</strong> or directly in the cell header. It names the earlier code cells this one depends on.</p></li><li><p><strong>🔗 link mode</strong> in the DAG pane — click it, then click two cells to draw a manual edge (shown <strong>dashed</strong>); click a dashed edge to remove it.</p></li></ul><p>Manual edges are first-class: they drive staleness, run order, and cache keys exactly like inferred ones. A <code>needs=</code> that names no earlier code cell (deleted, moved below, or a markdown cell) is inert and flagged with a <strong>🔗⚠</strong> badge on the cell.</p><h2 id="regions" tabindex="-1">Regions <a class="header-anchor" href="#regions" aria-label="Permalink to &quot;Regions&quot;">​</a></h2><p>A <strong>region</strong> (destination) is a <em>second kernel</em> — usually on another machine — where <strong>some</strong> of a notebook&#39;s cells run, while the rest stay on the main kernel. It lets you keep light cells local and push the heavy ones (a GPU model, a large query) to a beefy host, all in <strong>one</strong> notebook.</p><ul><li><p>Tag a cell <strong><code>region=&lt;name&gt;</code></strong> — or the bare <strong><code>remote</code></strong> tag, which is sugar for the <code>default</code> region — and it executes on that region&#39;s kernel. <strong>Boundary values cross automatically</strong> as content-addressed blobs; you don&#39;t move data by hand.</p></li><li><p>Declare destinations in the <strong>Destinations manager</strong> (the per-cell <em>Run on</em> picker in the 🏷 tag editor, and a notebook-level declare/remove). Point a region at a <a href="/KaimonSlate.jl/dev/remotes#warm-pools">warm pool</a> so it <strong>adopts</strong> a ready worker instead of paying a cold boot.</p></li><li><p>The DAG pane&#39;s <strong>🖧 region map</strong> colors cells by where they run, with a legend naming the hosts, and a cell wears a <strong>🖧</strong> badge when its last run executed on a region kernel.</p></li></ul><p>Regions build directly on the remote-execution machinery — host setup, transports, and the data-transfer settings that govern how boundary values cross the wire all live in <a href="./remotes">Remotes &amp; Pools</a>.</p><div class="tip custom-block"><p class="custom-block-title">Regions are new and evolving</p><p>Named regions and the DAG region map are a recent addition — the surface may still shift. The single-worker path (a whole notebook <a href="/KaimonSlate.jl/dev/remotes#run-a-notebook-on-a-remote">placed on one host</a>) is the stable option when you want the entire notebook elsewhere.</p></div><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><p><a href="./reactivity">Reactive Cells</a> — the reactive model the graph drives, and the 🔗 upstream focus.</p></li><li><p><a href="./remotes">Remotes &amp; Pools</a> — hosts, transports, warm pools, and the data-transfer settings regions rely on.</p></li><li><p><a href="./cell-tags">Cell Tags &amp; Caching</a> — the <code>needs=</code>, <code>remote</code>, and <code>region=</code> tags in the header.</p></li></ul>',18)])])}const u=a(s,[["render",r]]);export{p as __pageData,u as default};
