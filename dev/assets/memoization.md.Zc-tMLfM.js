import{_ as t,o,c as a,a2 as s}from"./chunks/framework.B08aFN1B.js";const p=JSON.parse('{"title":"Memoization & Caching","description":"","frontmatter":{},"headers":[],"relativePath":"memoization.md","filePath":"memoization.md","lastUpdated":null}'),r={name:"memoization.md"};function n(l,e,c,i,d,h){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="Memoization-and-Caching" tabindex="-1">Memoization &amp; Caching <a class="header-anchor" href="#Memoization-and-Caching" aria-label="Permalink to &quot;Memoization &amp; Caching {#Memoization-and-Caching}&quot;">​</a></h1><p>KaimonSlate persists expensive cell results to a <strong>durable, content-addressed store on disk</strong>. When you reopen a notebook — or its worker restarts — results <strong>restore instead of recomputing</strong>, so a pipeline that takes minutes to build comes back instantly. It&#39;s automatic for expensive cells, tunable per cell, and it invalidates <em>precisely</em> when a cell&#39;s real inputs change.</p><p>This is what makes a long-lived notebook feel warm: edit one cell and only its dependents recompute (see <a href="./reactivity">Reactive Cells</a>); reopen tomorrow and nothing recomputes at all.</p><div class="tip custom-block"><p class="custom-block-title">Gate workers only</p><p>The durable store lives in the notebook&#39;s <strong>gate worker</strong>. A notebook running <a href="/KaimonSlate.jl/dev/configuration#kernel-selection">in-process</a> (no worker) has no durable cache — its results live only as long as the session.</p></div><h2 id="The-durable-store" tabindex="-1">The durable store <a class="header-anchor" href="#The-durable-store" aria-label="Permalink to &quot;The durable store {#The-durable-store}&quot;">​</a></h2><p>The cache is a content-addressed store under your cache home (<code>$XDG_CACHE_HOME/kaimonslate/memo</code>, falling back to <code>~/.cache/…</code>). It has two kinds of file:</p><ul><li><p><strong>Manifests</strong> — one small TOML per cache key. A manifest records <em>which</em> globals a cell produced and, for each, a reference to its value blob. Manifests are <code>ls</code>-able and human-readable.</p></li><li><p><strong>Blobs</strong> — the actual value bytes, each named by the SHA-256 of its content. Because a blob is keyed by content, <strong>identical values dedup to a single blob</strong> — across cells, and even across notebooks. Re-keying an entry (e.g. after a <code>src/</code> edit) rewrites only the ~1 KB manifest; the data bytes are already there.</p></li></ul><p>The store is <strong>bounded</strong>: an LRU cap (set <code>KAIMONSLATE_MEMO_CAP_GB</code>, else an adaptive fraction of free disk) evicts the oldest entries first, and a blob is deleted only when no surviving manifest references it.</p><h2 id="What-gets-cached,-and-when" tabindex="-1">What gets cached, and when <a class="header-anchor" href="#What-gets-cached,-and-when" aria-label="Permalink to &quot;What gets cached, and when {#What-gets-cached,-and-when}&quot;">​</a></h2><ul><li><p><strong>Automatically</strong> — an error-free cell whose run takes more than a moment (about 150 ms) is cached without you doing anything. Fast cells never pay the serialization cost.</p></li><li><p><strong><code>cache</code> tag</strong> — opt <strong>in</strong> regardless of runtime. Persist a pipeline stage&#39;s result so it always restores instead of recomputing, even when the cell itself is quick. Ideal for a deterministic stage whose <em>inputs</em> rarely change.</p></li><li><p><strong><code>nocache</code> tag</strong> — opt <strong>out</strong>, for impure or side-effecting cells (randomness, wall-clock, a network write). A <code>nocache</code> cell is never memoized — and, correctly, it <strong>poisons the cache keys of everything downstream</strong>, because restoring a downstream result against a producer that must re-run would resurrect stale data.</p></li></ul><p>Set these with the 🏷 tag editor or a <code>#%%</code> header token — see <a href="/KaimonSlate.jl/dev/cell-tags#caching">Cell Tags</a>. Markdown cells, <code>using</code>/<code>import</code> barriers, notebook-local function definitions, and <a href="./cell-tags"><code>resource</code></a> cells (live DB/file/socket handles) are never cached.</p><h2 id="Cache-keys-—-what-invalidates-a-result" tabindex="-1">Cache keys — what invalidates a result <a class="header-anchor" href="#Cache-keys-—-what-invalidates-a-result" aria-label="Permalink to &quot;Cache keys — what invalidates a result {#Cache-keys-—-what-invalidates-a-result}&quot;">​</a></h2><p>A cell&#39;s result restores only when <strong>all</strong> of its real inputs are unchanged. The key is a hash of:</p><ul><li><p>the cell&#39;s own source, and the source of <strong>every cell upstream of it</strong>;</p></li><li><p>the current values of any <code>@bind</code> controls the cell reads (moving a slider re-keys);</p></li><li><p>the contents of any <code>@asset</code> files it (or an upstream cell) references;</p></li><li><p>the notebook&#39;s <strong>package versions</strong>;</p></li><li><p>the <strong>developed <code>src/</code></strong> of the project — so editing a function a cell calls invalidates the entry.</p></li></ul><p>Change any of those and the cell recomputes; change nothing and it restores. Set <code>KAIMONSLATE_MEMO_DEBUG=1</code> to log the key components.</p><div class="tip custom-block"><p class="custom-block-title">Assert two values are really the same — <code>slate_fingerprint</code></p><p><code>slate_fingerprint(xs...)</code> returns a canonical content hash with <code>isequal</code> semantics (order-independent Dicts/Sets, <code>NaN ≡ NaN</code>, <code>missing ≠ nothing</code>). It&#39;s the robust way to check a restored / recomputed / transferred value is identical across runs, sessions, and workers.</p></div><h2 id="Restore-on-reopen-or-restart" tabindex="-1">Restore on reopen or restart <a class="header-anchor" href="#Restore-on-reopen-or-restart" aria-label="Permalink to &quot;Restore on reopen or restart {#Restore-on-reopen-or-restart}&quot;">​</a></h2><p>On a cold open or a worker restart, before a cell runs its source, the worker looks for a manifest matching the cell&#39;s key and, if every blob is present, <strong>restores the values into the namespace — no recompute</strong>. Restored cells are surfaced as such (a &quot;restored&quot; state, logged as &quot;restored (no recompute)&quot;). Provider cells (<code>using</code>/<code>import</code>), <code>@bind</code> cells, and theme setters restore and then <strong>replay</strong> just their cheap side effects, so downstream cells see the right scope and theme.</p><p>The <strong>▶ play</strong> button on a cell forces a fresh recompute (skips restore and re-stores the result).</p><h2 id="Seeing-what&#39;s-cached" tabindex="-1">Seeing what&#39;s cached <a class="header-anchor" href="#Seeing-what&#39;s-cached" aria-label="Permalink to &quot;Seeing what&amp;#39;s cached {#Seeing-what&#39;s-cached}&quot;">​</a></h2><p>Two helpers, callable from a cell:</p><ul><li><p><strong><code>slate_memo_stats()</code></strong> → <code>(; manifests, blobs, bytes, root)</code> — entry count, unique content blobs (the dedup shows here), total on-disk bytes, and the store root.</p></li><li><p><strong><code>slate_memo_entries(; name=&quot;&quot;)</code></strong> → a <code>Vector</code> of rows, newest first: <code>(; key, names, bytes, blobs, elided, created)</code> — the globals each entry restores and their (shared-when-deduped) content hashes. <code>slate_table(slate_memo_entries())</code> renders exactly what a cold open will restore; <code>name=&quot;x&quot;</code> filters to entries carrying a binding <code>x</code>.</p></li></ul><p>And to answer <strong>&quot;why did this cell (not) restore?&quot;</strong> directly:</p><ul><li><strong><code>slate_memo_trace(notebook; cell=&quot;&quot;)</code></strong> → what the cache <em>did</em> on each cell&#39;s latest eval: the action (<strong>restored / stored / recomputed / unkeyed</strong>), the full cache key with its source- and package-digest components (compare across hosts to spot a key drift), the per-binding blobs read or written, and the exact <strong>miss reason</strong> on a recompute (no manifest · blob missing · decode failed · below threshold · …). Reads the live worker, so run a cell first.</li></ul><h2 id="Display-objects-are-elided" tabindex="-1">Display objects are elided <a class="header-anchor" href="#Display-objects-are-elided" aria-label="Permalink to &quot;Display objects are elided {#Display-objects-are-elided}&quot;">​</a></h2><p>Makie/Plots figures are pathological to serialize — a huge scene graph, seconds of work — for a chart whose rendered <strong>pixels already ride the wire</strong>. So when nothing downstream reads a display object, KaimonSlate <strong>elides</strong> it: the manifest keeps only the rendered image, not the object. It self-heals — if you later add a cell that <em>reads</em> the figure, the entry misses and re-stores the real object. Elided names show up in <code>slate_memo_entries</code> (the <code>elided</code> column).</p><h2 id="Arrow-tables-and-codecs" tabindex="-1">Arrow tables and codecs <a class="header-anchor" href="#Arrow-tables-and-codecs" aria-label="Permalink to &quot;Arrow tables and codecs {#Arrow-tables-and-codecs}&quot;">​</a></h2><div class="tip custom-block"><p class="custom-block-title">Newer, still settling</p><p>The typed codecs below are a recent optimization. Behavior is accurate to the current code but may evolve; the universal fallback always applies, so correctness never depends on them.</p></div><p>A value becomes blob bytes through one of three <strong>codecs</strong>, chosen automatically by type:</p><table tabindex="0"><thead><tr><th style="text-align:right;">You return</th><th style="text-align:right;">Codec</th><th style="text-align:right;">On restore</th><th style="text-align:right;">External-readable</th></tr></thead><tbody><tr><td style="text-align:right;">a <strong><code>DataFrame</code></strong> (with <code>DataFrames</code> <strong>and</strong> <code>Arrow</code> loaded)</td><td style="text-align:right;">Arrow IPC</td><td style="text-align:right;">memory-mapped off disk, zero-copy</td><td style="text-align:right;"><strong>yes</strong> — the blob is a valid Arrow file</td></tr><tr><td style="text-align:right;">a non-empty <strong>isbits <code>Array</code></strong> (<code>Vector{Float64}</code>, <code>Matrix{Int}</code>, …)</td><td style="text-align:right;">raw</td><td style="text-align:right;">memory-mapped, zero-copy</td><td style="text-align:right;">no (internal format)</td></tr><tr><td style="text-align:right;">anything else</td><td style="text-align:right;">Julia <code>Serialization</code> (<code>jls</code>)</td><td style="text-align:right;">deserialized</td><td style="text-align:right;">no</td></tr></tbody></table><p>The <code>jls</code> fallback handles <em>any</em> Julia value, so caching always works; the typed codecs just make large tabular/array data reopen and transfer far faster.</p><ul><li><p><strong>To get the fast path for a table, return a <code>DataFrame</code>.</strong> An <code>Arrow.Table</code>, a <code>Vector</code> of <code>NamedTuple</code>s, or a <code>SlateTable</code> falls back to <code>jls</code> — only a <code>DataFrame</code> triggers the Arrow codec. It requires <code>using DataFrames, Arrow</code> in the notebook (both); without them a <code>DataFrame</code> still caches correctly, just as <code>jls</code>.</p></li><li><p><strong>Zero-copy</strong> restore (an mmap-backed view) is used only when the dependency graph proves nothing downstream <strong>mutates</strong> the value. If you then mutate a zero-copy-restored value you&#39;ll get a <code>ReadOnlyMemoryError</code> — a safe failure; re-running the producer re-stores it mutably.</p></li><li><p>The Arrow blob is a plain Arrow IPC file on disk, so its bytes are readable by external tools (DuckDB, pyarrow). Note this is a property of the stored bytes — KaimonSlate doesn&#39;t currently surface the blob path for you.</p></li><li><p><strong>These codecs also power <a href="/KaimonSlate.jl/dev/regions#how-boundary-values-cross">region boundary transfers</a></strong> — a <code>DataFrame</code> crosses between kernels as Arrow IPC, moved zero-copy over the data channel.</p></li></ul><h2 id="Your-cache-follows-you" tabindex="-1">Your cache follows you <a class="header-anchor" href="#Your-cache-follows-you" aria-label="Permalink to &quot;Your cache follows you {#Your-cache-follows-you}&quot;">​</a></h2><p>When a notebook runs on a <a href="./remotes">remote worker</a>, <code>slate_sync_memo</code> pushes the whole store to it (dedup-aware) so the remote <strong>restores</strong> cached results instead of recomputing them. On attach, recent entries are carried automatically when moving them beats recomputing. See <a href="/KaimonSlate.jl/dev/remotes#your-cache-follows-you">Remotes → Your cache follows you</a>.</p><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><p><a href="./cell-tags">Cell Tags &amp; Caching</a> — the <code>cache</code> / <code>nocache</code> / <code>resource</code> tags in the header.</p></li><li><p><a href="./reactivity">Reactive Cells</a> — the dependency graph that defines a cell&#39;s inputs (and its key).</p></li><li><p><a href="./remotes">Remotes</a> — carrying the cache to another machine.</p></li></ul>',36)])])}const g=t(r,[["render",n]]);export{p as __pageData,g as default};
