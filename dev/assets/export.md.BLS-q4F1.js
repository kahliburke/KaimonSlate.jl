import{_ as t,o,c as s,a2 as r}from"./chunks/framework.BXm-Zw-h.js";const g=JSON.parse('{"title":"Export","description":"","frontmatter":{},"headers":[],"relativePath":"export.md","filePath":"export.md","lastUpdated":null}'),a={name:"export.md"};function n(i,e,d,l,c,p){return o(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="export" tabindex="-1">Export <a class="header-anchor" href="#export" aria-label="Permalink to &quot;Export&quot;">​</a></h1><p>KaimonSlate exports a notebook as a self-contained <strong>HTML</strong> page, a publication-quality <strong>PDF</strong> (rendered server-side with Typst), <strong>Markdown</strong>, a fully reproducible <strong>self-contained <code>.jl</code></strong>, and an <a href="./app-mode"><strong>app</strong></a> folder someone else can run.</p><p>One <strong>Export</strong> entry in the ☰ menu opens a dialog where you pick the format and its options. Your choices are remembered between exports.</p><h2 id="Export-HTML" tabindex="-1">Export HTML <a class="header-anchor" href="#Export-HTML" aria-label="Permalink to &quot;Export HTML {#Export-HTML}&quot;">​</a></h2><p>A single file that opens with no server behind it. What lands in it:</p><ul><li><p>Markdown cells rendered (GFM tables, KaTeX math).</p></li><li><p>Code cells with their source and output (values, stdout, errors).</p></li><li><p>Makie figures embedded as base64 images.</p></li><li><p>ECharts figures embedded as their specifications and re-drawn in the browser, so hover, zoom, legend toggles and tooltips keep working.</p></li><li><p>Tables with their sort, filter, paging and CSV download intact.</p></li><li><p><code>@bind</code> controls rendered as real controls, but disabled. Marking what they drive with <a href="./replay"><code>@replay</code></a> ships results for every position of the control and makes them work.</p></li></ul><p>Options for this format:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Option</th><th style="text-align:right;">What it does</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>Outputs</strong></td><td style="text-align:right;">All, figures only, or prose and code only.</td></tr><tr><td style="text-align:right;"><strong>Theme</strong></td><td style="text-align:right;">As-is, Light, or Dark. Overriding re-renders every figure for the export.</td></tr><tr><td style="text-align:right;"><strong>Chart renderer</strong></td><td style="text-align:right;">Auto (each chart&#39;s own setting), Canvas, or SVG. SVG is vector, so it stays crisp in print. A reader can override it with <code>?renderer=svg</code> on the URL.</td></tr><tr><td style="text-align:right;"><strong>Code size</strong></td><td style="text-align:right;">Normal down to tiny.</td></tr><tr><td style="text-align:right;"><strong>Page width</strong></td><td style="text-align:right;">The content column, from 620px to full width.</td></tr><tr><td style="text-align:right;"><strong>Source</strong></td><td style="text-align:right;">Include cell sources, or export outputs only.</td></tr><tr><td style="text-align:right;"><strong>Offline</strong></td><td style="text-align:right;">Inline KaTeX, ECharts and the widget stack instead of linking a CDN, so the page renders with no network at all. Adds roughly a megabyte.</td></tr><tr><td style="text-align:right;"><strong>Data</strong></td><td style="text-align:right;">Gzip the inlined libraries and data. Typically halves the file, and needs Chrome 80, Safari 16.4 or Firefox 113 to inflate. Numeric arrays are narrowed to 32-bit either way.</td></tr><tr><td style="text-align:right;"><strong>Runnable</strong></td><td style="text-align:right;">Embed the reproducible bundle and a &quot;Run live&quot; button, so a reader can run the notebook on their own machine.</td></tr><tr><td style="text-align:right;"><strong>Git history</strong></td><td style="text-align:right;">With Runnable on, ship the project&#39;s full history so a reader can branch and open a pull request with matching commits. Off ships the current source only, which is the safer default for a public page.</td></tr><tr><td style="text-align:right;"><strong>Precomputed results</strong></td><td style="text-align:right;">Embed cached cell results so a reader who runs it live skips the expensive cells. Entries are chosen by compute saved per byte.</td></tr><tr><td style="text-align:right;"><strong>Interim preview</strong></td><td style="text-align:right;">Embed the last rendered figures so they show immediately while the live environment rebuilds behind them.</td></tr></tbody></table><p><strong>🔗 Share via Gist</strong> uploads the export as a secret GitHub gist and copies a download command to your clipboard. It needs the <code>gh</code> CLI, authenticated.</p><p>If the notebook has any <code>@replay</code> marks, confirming opens a second step for choosing how finely to sweep each control. See <a href="./replay">Offline interactivity</a>.</p><p>The route is <code>GET /api/&lt;id&gt;/export.html</code> (<code>?dl=1</code> to download, <code>?source=0</code> for an output-only document).</p><h2 id="Publication-PDF-Typst" tabindex="-1">Publication PDF (Typst) <a class="header-anchor" href="#Publication-PDF-Typst" aria-label="Permalink to &quot;Publication PDF (Typst) {#Publication-PDF-Typst}&quot;">​</a></h2><p>The <strong>PDF</strong> format renders a typeset document <strong>server-side via Typst</strong> — not a browser print. It offers:</p><ul><li><p><strong>Theme</strong> — <em>As-is</em>, <em>Light</em> (publication), or <em>Dark</em>. <strong>As-is</strong> reuses each figure exactly as it&#39;s already rendered in the live UI (fastest — nothing re-renders). <strong>Light</strong> and <strong>Dark</strong> force a canonical palette and <strong>re-render every figure for the export</strong> so the whole document is internally consistent regardless of the live theme: native <strong>Makie</strong> figures are re-rendered on the worker under the chosen palette, and <strong>ECharts</strong> are re-drawn in the matching chart theme. (The dialog flags an override, since re-rendering takes a moment.)</p></li><li><p><strong>Layout</strong> — <em>Article</em> / <em>Report</em> × <em>single</em> / <em>two-column</em> (the title and abstract span the full width above the columns), or <em>Slides</em> for a 16:9 presentation deck.</p></li><li><p><strong>Speaker notes</strong> — on a slides layout, append one notes page per slide that has <code>notes</code> cells.</p></li><li><p><strong>Body text</strong> — <em>Auto</em> (compact for two-column), <em>Large</em>, <em>Normal</em>, <em>Compact</em>, <em>Small</em>.</p></li><li><p><strong>Code listings</strong> — <em>Normal</em> / <em>Small</em> / <em>Smaller</em> / <em>Tiny</em> font, or <em>Hidden</em> (outputs only).</p></li><li><p><strong>Parameters</strong> — show <code>@bind</code> controls as a strip at their current values. Off by default, so a PDF carries no controls unless you ask for them.</p></li><li><p><strong>Typst source</strong> — also download the editable Typst project as a <code>.tar.gz</code>: <code>doc.typ</code> plus its assets, which recompiles with <code>typst compile doc.typ</code> if you want to hand-finish the typesetting. Its route is <code>GET /api/&lt;id&gt;/export.typ</code>, taking the same options as <code>export.pdf</code>.</p></li></ul><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/export-dialog.png" alt="" style="width:520px;max-width:100%;"></p><p>Highlights:</p><ul><li><p><strong>Vector figures</strong> — CairoMakie figures embed as <strong>PDF</strong> (fonts embedded, crisp at any scale); ECharts charts embed as <strong>SVG</strong> in the export&#39;s theme. Rasters are the fallback.</p></li><li><p><strong>Math</strong> through LaTeX (<code>mitex</code>), with a shim preamble for commands <code>mitex</code> lacks. All four delimiter spellings work — <code>$…$</code>, <code>$$…$$</code>, <code>\\(…\\)</code>, <code>\\[…\\]</code> — the bracket forms are normalized to the dollar forms on the way in, so a notebook reads the same on screen and in print.</p></li><li><p><strong>Frozen controls</strong> — with <strong>Parameters</strong> ticked, <code>@bind</code> widgets render as a compact strip at their current values (a PDF is a snapshot).</p></li><li><p><strong>Academic front matter</strong> — if the first markdown cell opens with a <code>---</code>-fenced block, its <code>title</code> / <code>subtitle</code> / <code>author</code> / <code>date</code> / <code>abstract</code> render as a title block (the title overrides the filename) and the rest of that cell becomes body text.</p></li></ul><p>The route is <code>GET /api/&lt;id&gt;/export.pdf</code> with <code>?theme=</code>, <code>?style=</code>, <code>?columns=</code>, <code>?body=</code>, <code>?code=</code>. The bundled <code>Typst_jll</code> is used unless a system <code>typst</code> is on <code>PATH</code>.</p><h3 id="Writing-for-print-only" tabindex="-1">Writing for print only <a class="header-anchor" href="#Writing-for-print-only" aria-label="Permalink to &quot;Writing for print only {#Writing-for-print-only}&quot;">​</a></h3><p>You author in Markdown and LaTeX, not Typst — the <code>.typ</code> document is generated. For the cases where print needs something the screen doesn&#39;t, a markdown cell can address each surface on its own. Both markers are HTML comments, so neither disturbs the other view:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">&lt;!--raw-typst #pagebreak() --&gt;</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">&lt;!--typst-begin-exclude--&gt;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">This paragraph is for readers of the live notebook, and never reaches the PDF.</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">&lt;!--typst-end-exclude--&gt;</span></span></code></pre></div><p><code>raw-typst</code> passes its body through as <strong>Typst code</strong> — a page break, a <code>#place</code>, a <code>#set text(…)</code> — and the browser sees only an HTML comment, so it shows nothing. <code>typst-begin-exclude</code> is the mirror: the enclosed markdown renders normally on screen and is dropped from the PDF.</p><p>Only these two markers are honoured. Typst syntax written loose in a markdown cell is escaped and printed as ordinary text, since the cell is CommonMark first.</p><h2 id="Self-contained-single-source-.jl" tabindex="-1">Self-contained single-source <code>.jl</code> <a class="header-anchor" href="#Self-contained-single-source-.jl" aria-label="Permalink to &quot;Self-contained single-source `.jl` {#Self-contained-single-source-.jl}&quot;">​</a></h2><p>The <strong>Standalone .jl</strong> format produces one <code>.jl</code> that carries the notebook <strong>and</strong> its full environment, for sharing or archiving. The runnable cells are followed by a <code>Slate.bundle</code> footer embedding (gzip + base64):</p><ul><li><p><code>Project.toml</code> + <code>Manifest.toml</code> of the active environment (fully pinned),</p></li><li><p>the <strong>local / path-dependency source</strong> (the parent module code) under <code>local/&lt;pkg&gt;/</code>,</p></li><li><p>when the project is a git repo, a <strong>shallow git bundle</strong> (<code>repo.gitbundle</code>) + the <code>origin</code> URL — so an expanded copy can attach to the original remote with <strong>matching SHAs</strong>.</p></li></ul><p>Two options decide how it behaves when someone opens it:</p><ul><li><p><strong>Precomputed results</strong> — a size budget for embedded cached cell results, so an expensive notebook restores instead of recomputing. Entries are chosen by compute saved per byte, so even a small budget banks the most expensive work.</p></li><li><p><strong>Interim preview</strong> — embed the last rendered figures and tables so they appear the instant it opens, while the live environment rebuilds behind them. This is distinct from precomputed results, which restore the underlying values.</p></li></ul><p>Both are also offered for the HTML export&#39;s <strong>Runnable</strong> bundle.</p><p>A standalone <code>.jl</code> still opens as an ordinary notebook (the bundle footer is ignored on parse). To reinflate it into a project tree:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KaimonSlate</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;notebook.standalone.jl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → notebook.standalone.expanded/</span></span></code></pre></div><p><code>expand</code> writes <code>Project.toml</code> + <code>Manifest.toml</code>, the local source under <code>local/</code>, the runnable notebook, and — when a git bundle is present — <strong>auto-clones it into <code>repo/</code> with <code>origin</code> rewired</strong>, handing back a git repo whose tip SHA matches the original (branch &amp; PR straight away). It prints how to <code>Pkg.instantiate</code> the environment.</p><p>The route is <code>GET /api/&lt;id&gt;/export.standalone.jl</code>.</p><h2 id="markdown" tabindex="-1">Markdown <a class="header-anchor" href="#markdown" aria-label="Permalink to &quot;Markdown&quot;">​</a></h2><p>The <strong>Markdown</strong> format writes GitHub-flavored Markdown for pasting into Discourse, Slack or a GitHub issue, or saving as a <code>.md</code>. It offers whether to include cell sources as $```julia$ blocks, an image scale (full, max 800px, or max 480px, for hosts with paste size limits), and whether to name the file <code>README.md</code> so GitHub renders it.</p><p>Figures embed as data-URI images. GitHub does not render those, so upload a PNG alongside if the destination is a repository.</p><p>The route is <code>GET /api/&lt;id&gt;/export.md</code>.</p><h2 id="app" tabindex="-1">App <a class="header-anchor" href="#app" aria-label="Permalink to &quot;App&quot;">​</a></h2><p>The <strong>App</strong> format writes a folder that runs as an application, with launchers for macOS, Linux and Windows. See <a href="./app-mode">App Mode</a>.</p><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li><p>ECharts snapshots are captured from the live canvas as you view the notebook, so open a cell&#39;s chart at least once before exporting (HTML, PDF, or standalone) to ensure its snapshot is current.</p></li><li><p>A notebook&#39;s own front-end code is self-contained: <a href="./frontend-extensions">web cells</a>, <code>WebPage</code> output, and <code>@use</code> imports carry their HTML/CSS/JS (and import map) into the exported page, so custom widgets and scripts keep working offline.</p></li><li><p>Markdown chart / table interpolations (double-brace <code>echart(…)</code> / <code>slate_table(…)</code>) are client-hydrated and appear as static placeholders in HTML; scalar and image interpolations embed directly.</p></li></ul>',41)])])}const u=t(a,[["render",n]]);export{g as __pageData,u as default};
