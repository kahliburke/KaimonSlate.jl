import{_ as t,o,c as a,a2 as s}from"./chunks/framework.B08aFN1B.js";const u=JSON.parse('{"title":"Export","description":"","frontmatter":{},"headers":[],"relativePath":"export.md","filePath":"export.md","lastUpdated":null}'),n={name:"export.md"};function r(i,e,l,d,c,p){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="export" tabindex="-1">Export <a class="header-anchor" href="#export" aria-label="Permalink to &quot;Export&quot;">​</a></h1><p>KaimonSlate exports a notebook four ways: a self-contained <strong>HTML</strong> document, a publication-quality <strong>PDF</strong> (rendered server-side with Typst), browser <strong>Print HTML</strong>, and a fully reproducible <strong>self-contained <code>.jl</code></strong>.</p><h2 id="Export-HTML" tabindex="-1">Export HTML <a class="header-anchor" href="#Export-HTML" aria-label="Permalink to &quot;Export HTML {#Export-HTML}&quot;">​</a></h2><p><strong>☰ → Export HTML</strong> downloads a single <code>.html</code> file with everything inlined:</p><ul><li><p>Markdown cells rendered (GFM tables, KaTeX math).</p></li><li><p>Code cells with their source and output (values, stdout, errors).</p></li><li><p>Figures embedded as base64 — CairoMakie rasters directly, and client-rendered <strong>ECharts frozen to their latest snapshot PNG</strong>.</p></li><li><p>Interactive tables flattened to static HTML.</p></li></ul><p>No scripts to boot and no server required — the file opens offline, with KaTeX pulled from a CDN to typeset math. The route is <code>GET /api/&lt;id&gt;/export.html</code> (<code>?dl=1</code> to download, <code>?source=0</code> for an output-only document).</p><h2 id="Publication-PDF-Typst" tabindex="-1">Publication PDF (Typst) <a class="header-anchor" href="#Publication-PDF-Typst" aria-label="Permalink to &quot;Publication PDF (Typst) {#Publication-PDF-Typst}&quot;">​</a></h2><p><strong>☰ → Export PDF (publication)</strong> renders a typeset PDF <strong>server-side via Typst</strong> — not a browser print. A small dialog (remembered between exports) offers:</p><ul><li><p><strong>Theme</strong> — <em>Light</em> (publication) or <em>Dark</em> (matches the live UI; a natural fit for figures drawn with the Makie dark theme).</p></li><li><p><strong>Layout</strong> — <em>Article</em> / <em>Report</em> × <em>single</em> / <em>two-column</em> (the title and abstract span the full width above the columns).</p></li><li><p><strong>Body text</strong> — <em>Auto</em> (compact for two-column), <em>Large</em>, <em>Normal</em>, <em>Compact</em>, <em>Small</em>.</p></li><li><p><strong>Code listings</strong> — <em>Normal</em> / <em>Small</em> / <em>Smaller</em> / <em>Tiny</em> font, or <em>Hidden</em> (outputs only).</p></li></ul><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/export-dialog.png" alt=""></p><p>Highlights:</p><ul><li><p><strong>Vector figures</strong> — CairoMakie figures embed as <strong>PDF</strong> (fonts embedded, crisp at any scale); ECharts charts embed as <strong>SVG</strong> captured in both light and dark themes. Rasters are the fallback.</p></li><li><p><strong>Math</strong> through LaTeX (<code>mitex</code>), with a shim preamble for commands <code>mitex</code> lacks.</p></li><li><p><strong>Frozen controls</strong> — <code>@bind</code> widgets render as a compact <em>parameters</em> strip at their current values (a PDF is a snapshot).</p></li><li><p><strong>Academic front matter</strong> — if the first markdown cell opens with a <code>---</code>-fenced block, its <code>title</code> / <code>subtitle</code> / <code>author</code> / <code>date</code> / <code>abstract</code> render as a title block (the title overrides the filename) and the rest of that cell becomes body text.</p></li></ul><p>The route is <code>GET /api/&lt;id&gt;/export.pdf</code> with <code>?theme=</code>, <code>?style=</code>, <code>?columns=</code>, <code>?body=</code>, <code>?code=</code>. The bundled <code>Typst_jll</code> is used unless a system <code>typst</code> is on <code>PATH</code>.</p><h2 id="Print-HTML" tabindex="-1">Print HTML <a class="header-anchor" href="#Print-HTML" aria-label="Permalink to &quot;Print HTML {#Print-HTML}&quot;">​</a></h2><p><strong>☰ → Print HTML</strong> opens the static HTML document in a new tab and triggers your browser&#39;s print dialog — a quick path to PDF when you don&#39;t need the Typst typesetting.</p><h2 id="Self-contained-single-source-.jl" tabindex="-1">Self-contained single-source <code>.jl</code> <a class="header-anchor" href="#Self-contained-single-source-.jl" aria-label="Permalink to &quot;Self-contained single-source `.jl` {#Self-contained-single-source-.jl}&quot;">​</a></h2><p><strong>☰ → Export self-contained <code>.jl</code></strong> produces one <code>.jl</code> that carries the notebook <strong>and</strong> its full environment, for sharing or archiving. The runnable cells are followed by a <code>Slate.bundle</code> footer embedding (gzip + base64):</p><ul><li><p><code>Project.toml</code> + <code>Manifest.toml</code> of the active environment (fully pinned),</p></li><li><p>the <strong>local / path-dependency source</strong> (the parent module code) under <code>local/&lt;pkg&gt;/</code>,</p></li><li><p>when the project is a git repo, a <strong>shallow git bundle</strong> (<code>repo.gitbundle</code>) + the <code>origin</code> URL — so an expanded copy can attach to the original remote with <strong>matching SHAs</strong>.</p></li></ul><p>A standalone <code>.jl</code> still opens as an ordinary notebook (the bundle footer is ignored on parse). To reinflate it into a project tree:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KaimonSlate</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;notebook.standalone.jl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → notebook.standalone.expanded/</span></span></code></pre></div><p><code>expand</code> writes <code>Project.toml</code> + <code>Manifest.toml</code>, the local source under <code>local/</code>, the runnable notebook, and — when a git bundle is present — <strong>auto-clones it into <code>repo/</code> with <code>origin</code> rewired</strong>, handing back a git repo whose tip SHA matches the original (branch &amp; PR straight away). It prints how to <code>Pkg.instantiate</code> the environment.</p><p>The route is <code>GET /api/&lt;id&gt;/export.standalone.jl</code>.</p><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li><p>ECharts snapshots are captured from the live canvas as you view the notebook, so open a cell&#39;s chart at least once before exporting (HTML, PDF, or standalone) to ensure its snapshot is current.</p></li><li><p>Markdown chart / table interpolations (double-brace <code>echart(…)</code> / <code>slate_table(…)</code>) are client-hydrated and appear as static placeholders in HTML; scalar and image interpolations embed directly.</p></li></ul>',24)])])}const g=t(n,[["render",r]]);export{u as __pageData,g as default};
