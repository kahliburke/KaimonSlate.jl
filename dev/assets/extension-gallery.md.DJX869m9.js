import{_ as t,o as a,c as n,a2 as o}from"./chunks/framework.BXm-Zw-h.js";const p=JSON.parse('{"title":"The Extensions gallery","description":"","frontmatter":{},"headers":[],"relativePath":"extension-gallery.md","filePath":"extension-gallery.md","lastUpdated":null}'),s={name:"extension-gallery.md"};function i(r,e,l,h,d,g){return a(),n("div",null,[...e[0]||(e[0]=[o('<h1 id="The-Extensions-gallery" tabindex="-1">The Extensions gallery <a class="header-anchor" href="#The-Extensions-gallery" aria-label="Permalink to &quot;The Extensions gallery {#The-Extensions-gallery}&quot;">​</a></h1><p>Slate ships with controls, charts and tables built in. <strong>Extensions</strong> add the rest: 3-D globes, computer algebra, diagram languages, domain widgets. The gallery is where you find them and install them, without leaving the notebook.</p><p>Open it with <strong>⌘K → Extensions</strong>, or <strong>☰ → 🧩 Extensions</strong>.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/extensions-gallery.png" alt=""></p><h2 id="Finding-something" tabindex="-1">Finding something <a class="header-anchor" href="#Finding-something" aria-label="Permalink to &quot;Finding something {#Finding-something}&quot;">​</a></h2><p>The left column lists everything in the curated registry. Three things narrow it:</p><ul><li><p><strong>Search</strong> matches the name, tagline and description.</p></li><li><p><strong>Category chips</strong> (<code>visualization</code>, <code>controls</code>, <code>math</code>, …) filter to one kind of thing.</p></li><li><p><strong>All / Installed / Updates</strong> switch what the list is <em>about</em> — everything available, only what this notebook already has, or only what has a newer version waiting.</p></li></ul><p>Those last three are views over one list rather than separate screens, so an extension you&#39;ve installed is the same card in the same place, just with a different action on it.</p><p>Listings vary in how much their authors have filled in. Some carry a tagline, screenshots and a starter snippet; others show only the description harvested from their README. A sparse card isn&#39;t a worse package, only a quieter one.</p><h2 id="What-a-card-tells-you" tabindex="-1">What a card tells you <a class="header-anchor" href="#What-a-card-tells-you" aria-label="Permalink to &quot;What a card tells you {#What-a-card-tells-you}&quot;">​</a></h2><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/extensions-detail.png" alt=""></p><p>Alongside the description you may find:</p><ul><li><p><strong>Provides</strong> — the concrete things the extension adds: <code>@bind</code> controls, cell toolbar buttons, ⌘K commands, output renderers.</p></li><li><p><strong>Getting started</strong> — the few lines that put it to work.</p></li><li><p><strong>Example notebook</strong> — a runnable demo in the extension&#39;s own repository.</p></li></ul><h2 id="installing" tabindex="-1">Installing <a class="header-anchor" href="#installing" aria-label="Permalink to &quot;Installing&quot;">​</a></h2><p><strong>Install</strong> adds the package to <em>this notebook&#39;s</em> environment, exactly as the <a href="./packages">Packages panel</a> would. The notebook is paused while its worker resolves and precompiles — minutes, for something with a large dependency stack — then the cells re-run.</p><p>Two things are worth knowing before you click.</p><p><strong>Installing doesn&#39;t activate it.</strong> A package has to be loaded with <code>using</code>, so after a successful install the gallery offers to drop a starter cell in for you. Accepting is the fastest route from &quot;installed&quot; to &quot;working&quot;. If you decline, add <code>using ThePackage</code> yourself.</p><p><strong>The first install also adds a registry.</strong> Extensions come from a package registry that sits alongside Julia&#39;s General registry, and adding one affects <strong>every project on your machine</strong>, not just this notebook. The gallery says so before doing it, and the button reads <em>Add registry &amp; install</em> rather than <em>Install</em>. It&#39;s a one-time step; later installs don&#39;t repeat it.</p><p>On a notebook running in a <a href="./regions">region</a>, the registry is added to the <em>remote</em> depot — the one that notebook actually resolves against.</p><h2 id="Keeping-up-to-date" tabindex="-1">Keeping up to date <a class="header-anchor" href="#Keeping-up-to-date" aria-label="Permalink to &quot;Keeping up to date {#Keeping-up-to-date}&quot;">​</a></h2><p>An installed extension whose registry version has moved on is flagged with an amber <strong>update</strong> badge, and the <strong>Updates</strong> view collects them.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/extensions-updates.png" alt=""></p><p>The card shows the move it will make — <code>0.1.2 → 0.1.3</code> — and <strong>Update</strong> performs it, re-running the notebook afterwards. An extension inherited from the enclosing project is updated there rather than in the notebook&#39;s own environment.</p><p>Update information is only as fresh as the catalog, which is cached for several hours so that opening the gallery doesn&#39;t hit the network every time. <strong>Check for updates</strong> at the bottom refetches it immediately.</p><h2 id="When-you&#39;re-offline" tabindex="-1">When you&#39;re offline <a class="header-anchor" href="#When-you&#39;re-offline" aria-label="Permalink to &quot;When you&amp;#39;re offline {#When-you&#39;re-offline}&quot;">​</a></h2><p>The gallery still opens. It falls back to the last catalog it fetched, and failing that to the registry already in your Julia depot — which knows every extension&#39;s name and version, just not its description or screenshots. The footer says which of the three you&#39;re looking at, so an empty or sparse list is never ambiguous.</p><h2 id="What-an-extension-can-add" tabindex="-1">What an extension can add <a class="header-anchor" href="#What-an-extension-can-add" aria-label="Permalink to &quot;What an extension can add {#What-an-extension-can-add}&quot;">​</a></h2><p>Once loaded, an extension isn&#39;t confined to a corner of the UI. It can contribute:</p><ul><li><p><strong><code>@bind</code> controls</strong> that behave like the built-in ones</p></li><li><p><strong>Output renderers</strong>, so returning one of its values from a cell draws something</p></li><li><p><strong>Cell toolbar buttons</strong> on every cell&#39;s header</p></li><li><p><strong>⌘K commands</strong>, badged with the package name — so typing that name in the palette shows everything it added, which is the quickest way to find your way around something you just installed</p></li><li><p><strong>Markdown fence renderers</strong>, so a fenced block in prose renders through it</p></li></ul><h2 id="Where-they-come-from" tabindex="-1">Where they come from <a class="header-anchor" href="#Where-they-come-from" aria-label="Permalink to &quot;Where they come from {#Where-they-come-from}&quot;">​</a></h2><p>The registry is curated: an extension is listed because it was registered there. Installing one runs an ordinary <code>Pkg.add</code>, and the package is ordinary Julia code with the same access to your machine as anything else you install. Treat it with the same judgement.</p><p>If you want to publish your own, see <a href="./extensions">Writing an Extension</a> — registering the package is the only requirement to be listed.</p>',32)])])}const u=t(s,[["render",i]]);export{p as __pageData,u as default};
