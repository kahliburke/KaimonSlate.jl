import{_ as e,o as a,c as o,a2 as i}from"./chunks/framework.BXm-Zw-h.js";const u=JSON.parse('{"title":"Cell tags & caching","description":"","frontmatter":{},"headers":[],"relativePath":"cell-tags.md","filePath":"cell-tags.md","lastUpdated":null}'),r={name:"cell-tags.md"};function l(s,t,n,d,c,h){return a(),o("div",null,[...t[0]||(t[0]=[i('<h1 id="Cell-tags-and-caching" tabindex="-1">Cell tags &amp; caching <a class="header-anchor" href="#Cell-tags-and-caching" aria-label="Permalink to &quot;Cell tags &amp; caching {#Cell-tags-and-caching}&quot;">​</a></h1><p>Every cell carries a small set of <strong>tags</strong> in its <code>#%%</code> header. Tags change how a cell behaves, how it renders, and what role it plays in a document. Set them with the <strong>🏷</strong> button in the cell header (a small tag editor), or type a token directly into the header.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#%% code id=setup collapsed        ← a `collapsed` behaviour tag in the header</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LinearAlgebra, Statistics</span></span></code></pre></div><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/tag-editor.png" alt=""></p><h2 id="Caching-and-execution-tags" tabindex="-1">Caching and execution tags <a class="header-anchor" href="#Caching-and-execution-tags" aria-label="Permalink to &quot;Caching and execution tags {#Caching-and-execution-tags}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Tag</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><code>cache</code></td><td style="text-align:right;">Always persist the result, regardless of how long the cell took. See <a href="/KaimonSlate.jl/dev/cell-tags#caching">Caching</a>.</td></tr><tr><td style="text-align:right;"><code>nocache</code></td><td style="text-align:right;">Opt <strong>out</strong> of durable memoization, for impure or side-effecting cells that must re-run. Also stops everything downstream from being restored.</td></tr><tr><td style="text-align:right;"><code>resource</code></td><td style="text-align:right;">The cell opens a live external handle (a database connection, a file, a socket). It re-runs every time the notebook opens instead of restoring, and unlike <code>nocache</code> it leaves everything downstream cacheable.</td></tr><tr><td style="text-align:right;"><code>locked</code></td><td style="text-align:right;">Freeze the result. The cell never re-runs from an upstream change or a reload, only from its ▶ button, and its cache entry is pinned so eviction cannot drop it. See <a href="/KaimonSlate.jl/dev/cell-tags#Locking-a-result">Locking a result</a>.</td></tr><tr><td style="text-align:right;"><code>volatile</code></td><td style="text-align:right;">The cell&#39;s value changes on its own (wall-clock, a live feed). Like <code>nocache</code> it is never memoized and poisons downstream keys.</td></tr><tr><td style="text-align:right;"><code>trace</code></td><td style="text-align:right;">Wrap the cell in <code>@trace</code> — collect every intermediate value into a trace table (also the cell&#39;s 🔍 button). See <a href="/KaimonSlate.jl/dev/cell-tags#tracing">Tracing</a>.</td></tr><tr><td style="text-align:right;"><code>everywhere</code></td><td style="text-align:right;">Run this cell on every <a href="./regions">region</a> worker, not just the main one. For a cell that registers process-global state.</td></tr></tbody></table><p>If a cell returns a live handle without being tagged <code>resource</code>, the worker declines to cache it and says so, naming the tag.</p><h2 id="Display-tags" tabindex="-1">Display tags <a class="header-anchor" href="#Display-tags" aria-label="Permalink to &quot;Display tags {#Display-tags}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Tag</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><code>collapsed</code></td><td style="text-align:right;">Fold the cell (hide it in the notebook; omitted from slide/report export).</td></tr><tr><td style="text-align:right;"><code>hidecode</code></td><td style="text-align:right;">Hide the editor, show only the output — a clean result without the source.</td></tr></tbody></table><h2 id="Tags-that-take-a-value" tabindex="-1">Tags that take a value <a class="header-anchor" href="#Tags-that-take-a-value" aria-label="Permalink to &quot;Tags that take a value {#Tags-that-take-a-value}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Tag</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><code>needs=id1,id2</code></td><td style="text-align:right;">Assert a dependency on earlier cells that dataflow analysis cannot see. The case this exists for is coupling through something other than a variable, such as a cell reading a table another cell created. Only earlier code cells can be named.</td></tr><tr><td style="text-align:right;"><code>mutates=name1,name2</code></td><td style="text-align:right;">Assert that the cell modifies those values in place, when it does so through a function call or an alias that analysis cannot follow.</td></tr><tr><td style="text-align:right;"><code>region=&lt;name&gt;</code></td><td style="text-align:right;">Run this cell on a named compute target. See <a href="./regions">Regions</a>.</td></tr></tbody></table><p><code>needs=</code> is also what the DAG view&#39;s link gesture writes, so you can draw the edge instead of typing it.</p><p>Two more value tags, <code>lockedkey=</code> and <code>frozenat=</code>, are written by Slate itself to track a <code>locked</code> cell. Leave them alone.</p><h2 id="Presentation-tags" tabindex="-1">Presentation tags <a class="header-anchor" href="#Presentation-tags" aria-label="Permalink to &quot;Presentation tags {#Presentation-tags}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Tag</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><code>slide</code></td><td style="text-align:right;">Force a new <a href="./slides">slide</a> at this cell, regardless of headings.</td></tr><tr><td style="text-align:right;"><code>notes</code></td><td style="text-align:right;">Speaker notes — attached to the current slide, shown only in presenter/notes output.</td></tr></tbody></table><h2 id="Document-role-tags" tabindex="-1">Document-role tags <a class="header-anchor" href="#Document-role-tags" aria-label="Permalink to &quot;Document-role tags {#Document-role-tags}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Tag</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><code>title</code></td><td style="text-align:right;">This cell is the document <a href="/KaimonSlate.jl/dev/documents#Front-matter">title block</a>.</td></tr><tr><td style="text-align:right;"><code>abstract</code></td><td style="text-align:right;">This cell is the academic abstract.</td></tr><tr><td style="text-align:right;"><code>caption</code></td><td style="text-align:right;">This markdown cell is the figure caption for the output above it.</td></tr><tr><td style="text-align:right;"><code>bibliography</code></td><td style="text-align:right;">This cell is the <a href="/KaimonSlate.jl/dev/documents#bibliography">bibliography</a> (BibTeX or <code>.bib</code> paths).</td></tr></tbody></table><h2 id="Site-tags" tabindex="-1">Site tags <a class="header-anchor" href="#Site-tags" aria-label="Permalink to &quot;Site tags {#Site-tags}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Tag</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><code>home</code></td><td style="text-align:right;">This notebook is the published <a href="/KaimonSlate.jl/dev/publishing#Front-page-and-document-listing">site&#39;s front page</a>.</td></tr><tr><td style="text-align:right;"><code>docindex</code></td><td style="text-align:right;">Marks where the site&#39;s document listing is injected on a <code>home</code> notebook.</td></tr></tbody></table><h2 id="App-tags" tabindex="-1">App tags <a class="header-anchor" href="#App-tags" aria-label="Permalink to &quot;App tags {#App-tags}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Tag</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><code>workbook</code></td><td style="text-align:right;">In a <a href="/KaimonSlate.jl/dev/app-mode#Workbook-mode">workbook</a>, this code cell stays editable and runnable for the reader. Everything untagged stays read-only.</td></tr></tbody></table><p>There is no checkbox for <code>workbook</code> in the tag editor. Type it into the header, or add it through the editor&#39;s custom-tag input.</p><p>A token outside the set documented on this page is a <strong>free-form tag</strong> that round-trips in the <code>.jl</code> header without doing anything, which is useful for your own grouping or tooling.</p><h2 id="tracing" tabindex="-1">Tracing <a class="header-anchor" href="#tracing" aria-label="Permalink to &quot;Tracing&quot;">​</a></h2><p>Tagging a cell <code>trace</code> (or clicking its 🔍 button) wraps it in <code>@trace</code>: each line&#39;s value is collected into a <strong>trace table</strong> you can open and inspect, so you can see every intermediate without scattering <code>@show</code>. You can also write <code>@trace begin … end</code> by hand around part of a cell.</p><h2 id="caching" tabindex="-1">Caching <a class="header-anchor" href="#caching" aria-label="Permalink to &quot;Caching&quot;">​</a></h2><p>KaimonSlate durably <strong>memoizes</strong> cell results to disk: a cell that takes more than a moment (about 150 ms) is cached automatically, keyed by its source and inputs, and <strong>restored</strong> instead of recomputed after a worker restart or when you reopen the notebook. Two tags tune it:</p><ul><li><p><strong><code>cache</code></strong> — force a cell&#39;s result to persist regardless of runtime, for a deterministic pipeline stage whose inputs rarely change.</p></li><li><p><strong><code>nocache</code></strong> — opt out, for impure or side-effecting cells (randomness you want fresh, network calls) where a restored value would be wrong. It also stops everything downstream from being restored, since they depend on a value that must re-run.</p></li></ul><div class="tip custom-block"><p class="custom-block-title">Structure for the cache</p><p>Put an expensive computation (a simulation, a large read, a fit) in its <strong>own</strong> cell so its result is cached independently of the cheap cells that render it. The reactive engine already recomputes only what changed; the cache makes that survive restarts.</p></div><h2 id="Locking-a-result" tabindex="-1">Locking a result <a class="header-anchor" href="#Locking-a-result" aria-label="Permalink to &quot;Locking a result {#Locking-a-result}&quot;">​</a></h2><p>Some results are not reproducible from the code that made them: a benchmark, a training run, sampled data. <code>locked</code> freezes such a cell. It stops responding to upstream changes and to reopening the notebook, and re-runs only when you press its ▶ button. Slate records the cell&#39;s cache key as of the run it froze on and pins that entry, so the value survives both upstream drift and cache eviction.</p><p>Downstream cells key off the frozen identity rather than the cell&#39;s source, so two runs of identical code that produced different values do not collide.</p><p>Do not lock a cell carrying <a href="./replay"><code>@replay</code></a>. Such a cell has no computed key to pin, so it cannot be restored on reopen.</p><p>See <strong><a href="./memoization">Memoization &amp; Caching</a></strong> for the full model — the content-addressed store, cache keys, restore, display-object elision, and the Arrow/typed codecs.</p>',34)])])}const p=e(r,[["render",l]]);export{u as __pageData,p as default};
