import{_ as e,o,c as n,a2 as a}from"./chunks/framework.BXm-Zw-h.js";const p=JSON.parse('{"title":"Settings","description":"","frontmatter":{},"headers":[],"relativePath":"settings.md","filePath":"settings.md","lastUpdated":null}'),r={name:"settings.md"};function s(i,t,d,l,h,g){return o(),n("div",null,[...t[0]||(t[0]=[a('<h1 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to &quot;Settings&quot;">​</a></h1><p>Settings live in one dialog with two <strong>scopes</strong>. <strong>Global</strong> is your own preference, kept per browser and applied to every notebook. <strong>This notebook</strong> is the per-notebook override, and the pinned ones travel <em>in the <code>.jl</code></em> so the notebook reopens the same way anywhere.</p><p>Open it from <strong>☰ → ⚙ Settings</strong>, or ⌘K → <em>Settings</em>. ⌘K → <em>Settings: this notebook</em> opens the same dialog already on the second scope.</p><p>A section list runs down the side and a search box sits above it. The search covers both scopes and matches tooltips and placeholders as well as visible text, so typing &quot;vim&quot;, &quot;trackpad&quot; or &quot;svg&quot; finds the setting even when the word is not its label.</p><h2 id="global" tabindex="-1">Global <a class="header-anchor" href="#global" aria-label="Permalink to &quot;Global&quot;">​</a></h2><p>Four sections.</p><p><strong>Display</strong></p><table tabindex="0"><thead><tr><th style="text-align:right;">Setting</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>Full page width</strong></td><td style="text-align:right;">Use the full window width instead of the centered column.</td></tr><tr><td style="text-align:right;"><strong>Page width</strong></td><td style="text-align:right;">The content column&#39;s width, when not using full width.</td></tr><tr><td style="text-align:right;"><strong>Figure width</strong></td><td style="text-align:right;">How wide figures render, independently of the text column.</td></tr><tr><td style="text-align:right;"><strong>Chart scroll-zoom</strong></td><td style="text-align:right;">How much a wheel notch zooms a chart that has zooming enabled.</td></tr><tr><td style="text-align:right;"><strong>Wrap text output</strong></td><td style="text-align:right;">Soft-wrap long lines of cell output instead of scrolling them.</td></tr><tr><td style="text-align:right;"><strong>Wrap code editor</strong></td><td style="text-align:right;">Soft-wrap long lines in the editor.</td></tr><tr><td style="text-align:right;"><strong>Line numbers</strong></td><td style="text-align:right;">Show a line-number gutter in every code cell. Off by default — a cell is usually short enough not to need one. The Files tab always numbers its lines.</td></tr><tr><td style="text-align:right;"><strong>Indent guides</strong></td><td style="text-align:right;">Draw a hairline at each indent level, so a nested block&#39;s extent is visible at a glance.</td></tr><tr><td style="text-align:right;"><strong>Code folding</strong></td><td style="text-align:right;">Let Julia blocks be collapsed from the gutter: <code>function</code>, <code>struct</code>, <code>module</code>, <code>macro</code>, <code>if</code>, <code>for</code>, <code>while</code>, <code>try</code>, <code>let</code>, <code>begin</code>, <code>quote</code> and <code>do</code>. A folded block keeps its own header line and its <code>end</code>. Markdown cells have no grammar to fold.</td></tr></tbody></table><p><strong>Appearance</strong></p><table tabindex="0"><thead><tr><th style="text-align:right;">Setting</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>Theme</strong></td><td style="text-align:right;">The notebook UI&#39;s colour theme: Midnight (default), Graphite, Nord, Dracula, Solarized Dark, and the light Daylight and Solarized Light.</td></tr><tr><td style="text-align:right;"><strong>Chart renderer</strong></td><td style="text-align:right;">How interactive charts are drawn: <em>Auto</em> (each chart&#39;s own setting), <em>Canvas</em>, or <em>SVG</em>. See below.</td></tr><tr><td style="text-align:right;"><strong>Editor syntax</strong></td><td style="text-align:right;">Syntax-highlighting palette for the editor: Dark+, Monokai, Dracula, Nord, Tokyo Night, GitHub Dark, Gruvbox Dark, Solarized Dark, and the light One Light and Solarized Light.</td></tr></tbody></table><p><strong>Editing</strong></p><table tabindex="0"><thead><tr><th style="text-align:right;">Setting</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>Editor keymap</strong></td><td style="text-align:right;">Default, Vim or Emacs bindings in the cell editor. See below.</td></tr><tr><td style="text-align:right;"><strong>Live-update debounce</strong></td><td style="text-align:right;">Minimum delay (ms) between live recomputes while dragging a control. Higher = fewer recomputes on a slow kernel.</td></tr><tr><td style="text-align:right;"><strong>Autocomplete delay</strong></td><td style="text-align:right;">How long to wait before the completion popup opens.</td></tr><tr><td style="text-align:right;"><strong>Tab in autocomplete</strong></td><td style="text-align:right;">Whether ⇥ accepts the highlighted completion.</td></tr></tbody></table><h4 id="Saving-under-the-Vim-and-Emacs-keymaps" tabindex="-1">Saving under the Vim and Emacs keymaps <a class="header-anchor" href="#Saving-under-the-Vim-and-Emacs-keymaps" aria-label="Permalink to &quot;Saving under the Vim and Emacs keymaps {#Saving-under-the-Vim-and-Emacs-keymaps}&quot;">​</a></h4><p>A notebook has no save separate from execution, so &quot;write this buffer&quot; means <em>apply it</em> — and what that does depends on where you are:</p><table tabindex="0"><thead><tr><th style="text-align:right;">You are in</th><th style="text-align:right;"><code>:w</code> (vim) · <code>C-x C-s</code> (emacs) does</th></tr></thead><tbody><tr><td style="text-align:right;">a code cell</td><td style="text-align:right;">runs it</td></tr><tr><td style="text-align:right;">a markdown or <code>@bind</code> cell&#39;s source</td><td style="text-align:right;">commits the source and returns to the rendered view</td></tr><tr><td style="text-align:right;">a file in the <strong>Files</strong> tab</td><td style="text-align:right;">writes the file</td></tr></tbody></table><p>Vim also takes <code>:wq</code> and <code>:x</code> (apply, then leave the editor), <code>:q</code> (leave, keeping your edits, the same as clicking away) and <code>:q!</code> (discard the edit and leave — the one way to abandon it in a single action). ⌘S saves a file under every keymap.</p><p><strong>Agent</strong></p><table tabindex="0"><thead><tr><th style="text-align:right;">Setting</th><th style="text-align:right;">Effect</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>Agent model</strong></td><td style="text-align:right;">Sonnet / Opus / Haiku, any model served locally by Ollama or vmlx (both listed automatically, stored as <code>ollama:&lt;name&gt;</code> / <code>vmlx:&lt;name&gt;</code>), or a custom model id typed in.</td></tr><tr><td style="text-align:right;"><strong>Agent permissions</strong></td><td style="text-align:right;"><code>lab</code> / <code>auto</code> / <code>default</code> / <code>bypass</code> preset for the agent.</td></tr></tbody></table><p>Model and permission changes <a href="./agent">reap the agent</a> so the next message respawns on the new setting (the transcript is kept).</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/settings.png" alt="" style="width:520px;max-width:100%;"></p><h3 id="Chart-renderer" tabindex="-1">Chart renderer <a class="header-anchor" href="#Chart-renderer" aria-label="Permalink to &quot;Chart renderer {#Chart-renderer}&quot;">​</a></h3><p>ECharts draws to a canvas, and some browser and driver combinations fail to composite it. The chart then runs correctly behind a blank rectangle. Switching to <strong>SVG</strong> avoids that path.</p><p>Which renderer works depends on the browser doing the viewing, so this reader setting outranks the <a href="/KaimonSlate.jl/dev/visualization#Choosing-a-renderer"><code>renderer =</code></a> keyword an author put on a chart. Changing it rebuilds every chart on the page, because ECharts fixes the renderer when a chart is created.</p><p>A static export has no Settings dialog, so a reader who hits this can add <code>?renderer=svg</code> to the URL instead.</p><h2 id="This-notebook" tabindex="-1">This notebook <a class="header-anchor" href="#This-notebook" aria-label="Permalink to &quot;This notebook {#This-notebook}&quot;">​</a></h2><p>The second scope. Each setting starts from your global default and can be <strong>pinned to this notebook</strong>, and the pinned ones travel in the <code>.jl</code>.</p><p>A row that has not been pinned is badged <strong>default</strong> and says which value it is following, so you can tell an inherited setting from one this document has decided. The header says <strong>all defaults</strong> when nothing has been pinned at all.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/settings-notebook.png" alt=""></p><p>It holds:</p><ul><li><p><strong>Worker threads</strong> (<code>&quot;&lt;compute&gt;,&lt;interactive&gt;&quot;</code>) and <strong>Extra Julia flags</strong> (appended to this notebook&#39;s worker command line, e.g. <code>--gcthreads=4,1 --heap-size-hint=4G</code>). Changing either respawns the worker.</p></li><li><p><strong>Parallel cells</strong> — run independent cells concurrently.</p></li><li><p><strong>Hot-reload /src edits</strong> — whether edits to the project&#39;s own source re-enter the running kernel. See <a href="./hot-reload">Editing project source</a>.</p></li><li><p><strong>Macro-aware deps</strong> — expand unknown macros in the kernel to recover their real reads and writes. Off falls back to conservative static analysis, which is what you want for the rare macro with expansion-time side effects.</p></li><li><p><strong>Slides</strong> — heading level, transition, and PDF aspect ratio for a <a href="./slides">slide deck</a>.</p></li><li><p><strong>Bibliography style</strong> — see <a href="./documents">Documents &amp; Citations</a>.</p></li><li><p><strong>Agent model</strong> — override the global agent default for this notebook. (Agent permissions are a ⚙ Settings item, remembered locally and never written to the file.)</p></li><li><p><strong>Series</strong> — the publishing series this notebook belongs to.</p></li><li><p><strong>Replay resolution</strong> — read-only here, showing what the export dialog&#39;s <a href="/KaimonSlate.jl/dev/replay#exporting">replay step</a> decided. The panel is where you clear it.</p></li></ul><p>Whether a notebook runs its cells as it opens is not a config setting. It is chosen when the notebook is opened, from the front page&#39;s <strong>Launch worker on open</strong> checkbox.</p><p>Where a value crosses into other UI: the notebook&#39;s <strong>run location</strong> is the toolbar &quot;Running on&quot; picker (whole-notebook placement, <a href="./remotes">Remotes</a>), and its <strong>regions</strong> are the <a href="/KaimonSlate.jl/dev/regions#Using-a-region-in-a-notebook">Destinations</a> it enables — both are saved in the same config footer.</p>',32)])])}const m=e(r,[["render",s]]);export{p as __pageData,m as default};
