import{_ as a,o as t,c as s,a2 as i}from"./chunks/framework.BXm-Zw-h.js";const g=JSON.parse('{"title":"Installation","description":"","frontmatter":{},"headers":[],"relativePath":"installation.md","filePath":"installation.md","lastUpdated":null}'),n={name:"installation.md"};function o(l,e,r,h,d,p){return t(),s("div",null,[...e[0]||(e[0]=[i('<h1 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to &quot;Installation&quot;">​</a></h1><p>KaimonSlate runs best as a <strong><a href="https://github.com/kahliburke/Kaimon.jl" target="_blank" rel="noreferrer">Kaimon</a> extension</strong> — that&#39;s the full experience: each notebook evaluates in its own gate worker (clean namespace, package management, a tailable log) and the <strong>AI agent</strong> can drive the notebook through the <code>slate.*</code> tools. A lighter <strong>standalone</strong> mode works without Kaimon for a quick look.</p><p>Either way, you drive it with the <strong><code>slate</code> app</strong> — a small launcher, installed with Julia&#39;s app system, that starts (or attaches to) the notebook hub and shows a status TUI.</p><p><code>slate</code> is a <strong>command you run in your terminal</strong>, not Julia code: installing the app writes an executable launcher into Julia&#39;s app bin directory (<code>~/.julia/bin</code> by default), which you&#39;ll want on your <code>PATH</code>. Step 3 below covers that.</p><h2 id="requirements" tabindex="-1">Requirements <a class="header-anchor" href="#requirements" aria-label="Permalink to &quot;Requirements&quot;">​</a></h2><ul><li><p><strong>Julia 1.12 or newer</strong> — the <code>slate</code> app uses Julia&#39;s app system.</p></li><li><p>A browser (any modern Chromium/Firefox/Safari).</p></li><li><p><strong><a href="https://github.com/kahliburke/Kaimon.jl" target="_blank" rel="noreferrer">Kaimon</a></strong> (recommended) — for the gate workers and the AI agent. For the agent&#39;s models: a logged-in <code>claude</code> CLI (Claude models) and/or a running <a href="https://ollama.com" target="_blank" rel="noreferrer">Ollama</a> (local models).</p></li></ul><h2 id="Recommended-path" tabindex="-1">Recommended path <a class="header-anchor" href="#Recommended-path" aria-label="Permalink to &quot;Recommended path {#Recommended-path}&quot;">​</a></h2><h3 id="1.-Install-Kaimon" tabindex="-1">1. Install Kaimon <a class="header-anchor" href="#1.-Install-Kaimon" aria-label="Permalink to &quot;1. Install Kaimon {#1.-Install-Kaimon}&quot;">​</a></h3><p>Install Kaimon and make sure it runs (see the Kaimon docs). Log in the <code>claude</code> CLI and/or start Ollama if you want the agent. This gives KaimonSlate its gate workers and the agent runtime.</p><h3 id="2.-Install-the-slate-app" tabindex="-1">2. Install the <code>slate</code> app <a class="header-anchor" href="#2.-Install-the-slate-app" aria-label="Permalink to &quot;2. Install the `slate` app {#2.-Install-the-slate-app}&quot;">​</a></h3><p>KaimonSlate is in the <strong>General</strong> registry. Install it as an <strong>app</strong> — this puts a <code>slate</code> launcher on your <code>PATH</code>. From the Pkg REPL (press <code>]</code>):</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> app add KaimonSlate</span></span></code></pre></div><p>The SDK it builds on, <code>SlateExtensionsBase</code>, is in General too, so writing an extension needs nothing beyond the default registry.</p><p>Note the <code>app</code> prefix: an installed app lives in its own environment, so a plain <code>add KaimonSlate</code> acts on whatever environment is active and won&#39;t install the launcher.</p><div class="tip custom-block"><p class="custom-block-title">Upgrading from a pre-release install</p><p>Earlier instructions used <code>app dev</code> plus a manual <code>instantiate</code> of <code>~/.julia/dev/KaimonSlate</code>. That still works but pins you to whatever is in that clone. To switch, run <code>pkg&gt; app rm KaimonSlate</code>, add the registry, then <code>pkg&gt; app add KaimonSlate</code>. The old clone can be deleted.</p></div><h4 id="upgrading" tabindex="-1">Upgrading <a class="header-anchor" href="#upgrading" aria-label="Permalink to &quot;Upgrading&quot;">​</a></h4><p>Two steps, and the second one matters:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> registry update          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># registries are cached; a new version isn&#39;t visible until you refresh</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> app up KaimonSlate       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># `up` is short for `update`</span></span></code></pre></div><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slate</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                         # then run it once</span></span></code></pre></div><p>Running <code>slate</code> is what tells <strong>Kaimon</strong> about the upgrade. Each version installs into its own directory (<code>packages/KaimonSlate/&lt;slug&gt;</code>, named after the version&#39;s contents), while Kaimon&#39;s <code>extensions.json</code> still points at the previous one — which is usually still on disk, so nothing errors and the update simply appears not to have happened. Launching <code>slate</code> re-points that entry at the new install; a running Kaimon reloads the extension within a few seconds, and the TUI&#39;s <code>Version</code> row reports what the hub is actually serving.</p><p>If a package or version &quot;does not exist&quot; and you believe it should, <code>registry update</code> first — a stale registry is the most common cause.</p><h3 id="3.-Put-slate-on-your-PATH" tabindex="-1">3. Put <code>slate</code> on your <code>PATH</code> <a class="header-anchor" href="#3.-Put-slate-on-your-PATH" aria-label="Permalink to &quot;3. Put `slate` on your `PATH` {#3.-Put-slate-on-your-PATH}&quot;">​</a></h3><p>Julia&#39;s app system doesn&#39;t install a Julia function — it writes an <strong>executable launcher</strong> (a shell script on macOS/Linux, a <code>slate.bat</code> on Windows) into Julia&#39;s app bin directory, which is <code>bin/</code> under your first depot:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;"></th></tr></thead><tbody><tr><td style="text-align:right;">macOS / Linux</td><td style="text-align:right;"><code>~/.julia/bin/slate</code></td></tr><tr><td style="text-align:right;">Windows</td><td style="text-align:right;"><code>%USERPROFILE%\\.julia\\bin\\slate.bat</code></td></tr></tbody></table><p>That directory is <strong>not</strong> on your <code>PATH</code> by default, and <code>app add</code> prints a reminder to add it. Do that once in your shell profile:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ~/.zshrc, ~/.bashrc, …</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/.julia/bin:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$PATH</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span></code></pre></div><p>Then open a new terminal and check the shell resolves it:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">which</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> slate</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # → /Users/you/.julia/bin/slate</span></span></code></pre></div><p>Until it&#39;s on your <code>PATH</code> you can still run it by full path — <code>~/.julia/bin/slate</code> — which is a handy way to confirm the install worked.</p><h3 id="4.-Run-slate" tabindex="-1">4. Run <code>slate</code> <a class="header-anchor" href="#4.-Run-slate" aria-label="Permalink to &quot;4. Run `slate` {#4.-Run-slate}&quot;">​</a></h3><p><code>slate</code> is a <strong>terminal command</strong>, not a Julia function — run it at your shell prompt, not in the Julia REPL:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slate</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # start (or attach to) the hub + status TUI</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my_analysis.jl</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # also open that notebook in the browser (created if missing)</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title"><code>slate</code> at the <code>julia&gt;</code> prompt won&#39;t work</p><p>Typing <code>slate</code> in the Julia REPL raises <code>UndefVarError: slate not defined</code> — the REPL is looking for a Julia binding, and the app is a program on your <code>PATH</code>. Run it from your shell, or shell out from the REPL with <code>;</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ;slate my_analysis</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">jl</span></span></code></pre></div><p>To drive the hub <em>from</em> Julia instead, use the library API — <code>serve_notebook(&quot;nb.jl&quot;)</code>; see <strong>Embedding (programmatic)</strong> below.</p></div><p>The <strong>first time</strong> you run it with Kaimon installed, <code>slate</code> offers to register itself as a Kaimon extension:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Register Slate as a Kaimon extension now?  [Y]es · [n]o · [d]on&#39;t ask again</span></span></code></pre></div><p>Say <strong>yes</strong> — Kaimon scans for extensions dynamically, so it&#39;s picked up without a restart. <code>slate</code> stays open and <strong>attaches the moment Kaimon brings the hub up</strong>. Now your agents have the <code>slate.*</code> tools and <strong>Kaimon serves your notebooks from its own hub — no extra process</strong> — and running <code>slate</code> again <strong>attaches</strong> to that hub as a live status viewer.</p><div class="tip custom-block"><p class="custom-block-title">Consented, not automatic</p><p>Registration is a one-time prompt you approve — it replaces the old silent auto-register on package load. Nothing wires itself into Kaimon behind your back; <code>KaimonSlate.register_extension()</code> registers manually, and <code>slate</code> never overwrites a registration you removed.</p></div><h3 id="5.-Open-the-browser" tabindex="-1">5. Open the browser <a class="header-anchor" href="#5.-Open-the-browser" aria-label="Permalink to &quot;5. Open the browser {#5.-Open-the-browser}&quot;">​</a></h3><p><code>slate</code> opens your notebook automatically. Otherwise browse the hub index at <strong><code>http://127.0.0.1:8765</code></strong> (set <code>KAIMONSLATE_PORT</code> to move it) — it lists open notebooks and opens more by path — or just <strong>ask the 💬 agent</strong> to open one (<code>slate.open</code>). See <a href="./agent">The AI Agent</a> for the full tool surface.</p><h2 id="Extension-packages" tabindex="-1">Extension packages <a class="header-anchor" href="#Extension-packages" aria-label="Permalink to &quot;Extension packages {#Extension-packages}&quot;">​</a></h2><p>Extensions add output types, widgets, assets and agent context to a notebook. They are ordinary Julia packages built on <code>SlateExtensionsBase</code> (see <a href="./extensions">Writing an Extension</a>) — you <code>add</code> them to a <strong>notebook&#39;s environment</strong>, not to the app.</p><p>Packages that live in General install directly. The rest are published through <strong>SlateRegistry</strong>, a registry that sits alongside General; add it once per machine:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> registry add https</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">://</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">github</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">com</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">kahliburke</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SlateRegistry</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> add SlatePlotly      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># into the notebook&#39;s environment</span></span></code></pre></div><p>Pkg searches every installed registry, so adding it changes nothing about how ordinary packages resolve — KaimonSlate and <code>SlateExtensionsBase</code> still come from General.</p><p>Several of those extension repositories are <strong>private</strong>, and installing one needs git credentials for GitHub. The registry records repository locations as HTTPS URLs, so an SSH key alone is not enough:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> auth</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> login</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # choose HTTPS for the git protocol</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> auth</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> setup-git</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # writes the credential helper; a separate step, easily missed</span></span></code></pre></div><p>A newly published version isn&#39;t visible until <code>pkg&gt; registry update</code>.</p><h2 id="Standalone-without-Kaimon" tabindex="-1">Standalone (without Kaimon) <a class="header-anchor" href="#Standalone-without-Kaimon" aria-label="Permalink to &quot;Standalone (without Kaimon) {#Standalone-without-Kaimon}&quot;">​</a></h2><p><code>slate --own</code> runs the hub <strong>in-process</strong>, without Kaimon (also the default when Kaimon isn&#39;t installed):</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slate</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --own</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # own the hub locally, even if a Kaimon extension is registered</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slate</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --own</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> analysis.jl</span></span></code></pre></div><p>Cells evaluate, widgets and figures work, and the <a href="./history">Timeline</a> records history. The AI agent is unavailable (it needs Kaimon), and notebooks evaluate in-process rather than in a per-notebook worker.</p><h2 id="The-status-TUI" tabindex="-1">The status TUI <a class="header-anchor" href="#The-status-TUI" aria-label="Permalink to &quot;The status TUI {#The-status-TUI}&quot;">​</a></h2><p>However you start it, <code>slate</code> shows a terminal dashboard:</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/slate-tui.gif" alt=""></p><ul><li><p><strong>Server</strong> — whether the hub is <em>up</em> (this process owns it), <em>attached</em> (an external Kaimon-extension hub), or <em>waiting</em> for the extension to come up — plus the hub URL and whether Slate is registered with Kaimon.</p></li><li><p><strong>Notebooks</strong> — a live table of every open notebook: cells, running / stale / errored counts, its worker port, and URL.</p></li><li><p><strong>Keys</strong> — <code>↑↓</code>/<code>enter</code> open the selected notebook, <code>o</code> opens the hub index, <code>r</code> restarts the hub you own, <code>s</code> starts a local hub (when waiting on the extension), <code>q</code> quits.</p></li></ul><h2 id="Embedding-programmatic" tabindex="-1">Embedding (programmatic) <a class="header-anchor" href="#Embedding-programmatic" aria-label="Permalink to &quot;Embedding (programmatic) {#Embedding-programmatic}&quot;">​</a></h2><p>To drive the hub from your own script instead of the app, the REPL API is still available — <code>serve_notebook</code> / <code>start_server</code> / <code>stop_server</code> — see <a href="/KaimonSlate.jl/dev/configuration#serving">Configuration</a> and the <a href="./api">API Reference</a>.</p><div class="tip custom-block"><p class="custom-block-title">In-process vs. gate worker</p><p>Even standalone, a notebook gets its own <strong>gate worker</strong> when it sits inside a Julia project and Kaimon&#39;s gate is available — giving it a clean namespace, a tailable log, package management, and isolation from the server. Otherwise it evaluates in-process. See <a href="./architecture">Architecture</a>.</p></div><h2 id="Next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#Next-steps" aria-label="Permalink to &quot;Next steps {#Next-steps}&quot;">​</a></h2><ul><li><p><a href="./getting-started">Getting Started</a> — open your first notebook and build a few cells.</p></li><li><p><a href="./agent">The AI Agent</a> — hand work to the agent through the <code>slate.*</code> tools.</p></li><li><p><a href="./architecture">Architecture</a> — how the pieces fit together.</p></li></ul>',60)])])}const u=a(n,[["render",o]]);export{g as __pageData,u as default};
