import{_ as t,o,c as a,a2 as n}from"./chunks/framework.B08aFN1B.js";const u=JSON.parse('{"title":"The AI Agent","description":"","frontmatter":{},"headers":[],"relativePath":"agent.md","filePath":"agent.md","lastUpdated":null}'),r={name:"agent.md"};function s(i,e,l,d,h,c){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="The-AI-Agent" tabindex="-1">The AI Agent <a class="header-anchor" href="#The-AI-Agent" aria-label="Permalink to &quot;The AI Agent {#The-AI-Agent}&quot;">​</a></h1><p>When KaimonSlate runs under <a href="https://github.com/kahliburke/Kaimon.jl" target="_blank" rel="noreferrer">Kaimon</a>, the <strong>💬 agent</strong> pane drives the notebook through the <em>same</em> operations you use — adding, editing, running, and deleting cells one at a time. It is a live collaborator, not a file-rewriter: you watch each cell appear and run.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/agent-panel.png" alt="" style="width:380px;max-width:100%;"></p><h2 id="How-it-works" tabindex="-1">How it works <a class="header-anchor" href="#How-it-works" aria-label="Permalink to &quot;How it works {#How-it-works}&quot;">​</a></h2><p>The agent is a consumer of Kaimon&#39;s agent service. A chat turn is forwarded to an agent session bound to the notebook; the agent calls the <code>slate.*</code> tools; their effects flow through the normal reactive path, and the agent&#39;s event stream is relayed onto the notebook&#39;s SSE so the chat pane updates live (streaming text, tool calls, and figures).</p><h3 id="The-tool-surface" tabindex="-1">The tool surface <a class="header-anchor" href="#The-tool-surface" aria-label="Permalink to &quot;The tool surface {#The-tool-surface}&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:right;">Tool</th><th style="text-align:right;">Purpose</th></tr></thead><tbody><tr><td style="text-align:right;"><code>slate_read</code></td><td style="text-align:right;">the whole notebook — every cell&#39;s source + output/error</td></tr><tr><td style="text-align:right;"><code>slate_add_cell</code></td><td style="text-align:right;">append a cell, <strong>run it</strong>, return its result</td></tr><tr><td style="text-align:right;"><code>slate_edit_cell</code></td><td style="text-align:right;">revise a cell, run it, return its result</td></tr><tr><td style="text-align:right;"><code>slate_run</code></td><td style="text-align:right;">run a cell (or all stale)</td></tr><tr><td style="text-align:right;"><code>slate_delete_cell</code></td><td style="text-align:right;">remove a cell</td></tr><tr><td style="text-align:right;"><code>slate_view</code></td><td style="text-align:right;"><strong>see</strong> a cell&#39;s rendered figure (returns the image)</td></tr><tr><td style="text-align:right;"><code>slate_search_docs</code> / <code>slate_index_docs</code></td><td style="text-align:right;">semantic search of the notebook&#39;s package docs</td></tr></tbody></table><p>Tool calls are shown in the chat with friendly labels (e.g. <code>➕ add cell</code>, <code>🖼 view figure</code>) rather than raw <code>mcp__kaimon__…</code> names.</p><div class="tip custom-block"><p class="custom-block-title">The agent can see your plots <code>slate_view</code> returns the cell&#39;s figure as an image — CairoMakie rasters and ECharts canvas snapshots both flow through one interface — so the agent can inspect a chart it made and fix it. :::</p><h2 id="Scoping-a-turn-to-a-cell-—" tabindex="-1">Scoping a turn to a cell — ✨ <a class="header-anchor" href="#Scoping-a-turn-to-a-cell-—" aria-label="Permalink to &quot;Scoping a turn to a cell — ✨ {#Scoping-a-turn-to-a-cell-—}&quot;">​</a></h2><p>Click <strong>✨</strong> on a cell header to scope the next turn to that cell. The server sends the agent that cell&#39;s source and output, its <strong>upstream dependency cone</strong>, and its downstream impact — and instructs it to work <em>only</em> there (not survey the whole notebook). A &quot;focused on cell&quot; banner shows the scope; click ✕ to clear.</p><h2 id="Referencing-cells-—-@id" tabindex="-1">Referencing cells — @id <a class="header-anchor" href="#Referencing-cells-—-@id" aria-label="Permalink to &quot;Referencing cells — @id {#Referencing-cells-—-@id}&quot;">​</a></h2><p>Type <strong>@</strong> in the chat to autocomplete a cell id; picking one inserts <code>@id</code>. The server expands each <code>@id</code> mention into that cell&#39;s source and current result, so you can point the agent at specific cells without it reading everything.</p><h2 id="Models-and-permissions" tabindex="-1">Models and permissions <a class="header-anchor" href="#Models-and-permissions" aria-label="Permalink to &quot;Models and permissions {#Models-and-permissions}&quot;">​</a></h2><p>In <strong>⚙ Settings</strong>:</p><ul><li><p><strong>Agent model</strong> — Sonnet (default), Opus, Haiku, or any locally-installed <strong>Ollama</strong> model (listed automatically from your Ollama install). See <a href="./configuration">Configuration</a>.</p></li><li><p><strong>Agent permissions</strong> — <code>lab</code> (default: slate/ex/edit tools), <code>auto</code> (model self-governs), <code>default</code> (edits only), or <code>bypass</code> (no checks; trusted only).</p></li></ul><p>Both bind when the agent spawns, so changing either reaps the running agent (the transcript is kept) and the next message respawns on the new setting. A note appears in the chat; if a turn is in flight you&#39;re asked to confirm before it&#39;s interrupted.</p><h2 id="stopping" tabindex="-1">Stopping <a class="header-anchor" href="#stopping" aria-label="Permalink to &quot;Stopping&quot;">​</a></h2><p>The <strong>⏹ Stop</strong> button interrupts the in-flight turn. If the agent is wedged, a second press (<strong>⛔ Force stop</strong>) hard-kills it; the next message spawns a fresh agent (transcript kept).</p><h2 id="Conversation-history" tabindex="-1">Conversation history <a class="header-anchor" href="#Conversation-history" aria-label="Permalink to &quot;Conversation history {#Conversation-history}&quot;">​</a></h2><p>The transcript is buffered server-side and <strong>persisted to disk per notebook</strong>, so it survives both a browser reload and a server restart. The <strong>🧹</strong> button clears it (memory and disk) and stops the agent.</p><h2 id="Multiple-agents-on-one-notebook" tabindex="-1">Multiple agents on one notebook <a class="header-anchor" href="#Multiple-agents-on-one-notebook" aria-label="Permalink to &quot;Multiple agents on one notebook {#Multiple-agents-on-one-notebook}&quot;">​</a></h2><p>A build-floor + version-CAS layer lets several agents (e.g. a <a href="https://github.com/kahliburke/Seaworthy.jl" target="_blank" rel="noreferrer">Seaworthy</a> crew) drive one notebook without clobbering each other:</p><ul><li><p><strong>Build-floor</strong> — a notebook-scoped lease (<code>slate_acquire_floor</code> / <code>slate_release_floor</code>). While held, only the holder may commit; it auto-expires after idle so a crashed holder can&#39;t deadlock the notebook.</p></li><li><p><strong>Version-CAS</strong> — when no floor is held, a mutation may carry the <code>nb.version</code> it was decided against and is rejected if the notebook moved since (lost-update guard).</p></li></ul><p>Both are opt-in, so the solo-agent path is unaffected. Each crew member gets a colored lane in the chat.</p><h2 id="Working-without-Kaimon" tabindex="-1">Working without Kaimon <a class="header-anchor" href="#Working-without-Kaimon" aria-label="Permalink to &quot;Working without Kaimon {#Working-without-Kaimon}&quot;">​</a></h2><p>In standalone mode (no Kaimon) the chat pane reports that the agent service is unavailable — everything else (cells, widgets, figures, history, export) works.</p></div>',9)])])}const p=t(r,[["render",s]]);export{u as __pageData,p as default};
