import{_ as o,o as t,c as a,a2 as n}from"./chunks/framework.BtKjyiyt.js";const p=JSON.parse('{"title":"Regions — split a notebook across kernels","description":"","frontmatter":{},"headers":[],"relativePath":"regions.md","filePath":"regions.md","lastUpdated":null}'),r={name:"regions.md"};function s(i,e,l,d,g,c){return t(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="Regions-—-split-a-notebook-across-kernels" tabindex="-1">Regions — split a notebook across kernels <a class="header-anchor" href="#Regions-—-split-a-notebook-across-kernels" aria-label="Permalink to &quot;Regions — split a notebook across kernels {#Regions-—-split-a-notebook-across-kernels}&quot;">​</a></h1><p>A notebook runs on its <strong>main kernel</strong> (local, by default). With <strong>regions</strong> you send <em>some</em> of its cells to a <strong>second kernel on another host</strong> — while the rest stay local — all in one notebook. Boundary values cross automatically, so you can keep light cells local and push the heavy ones (a GPU model, a big SQL query, a memory-hungry join) to a beefy remote, without splitting your work into separate files.</p><p>Regions are <strong>named compute definitions</strong> managed globally — a host, a transport, a data root, and how many workers to keep warm. Host setup and transports live in <a href="./remotes">Remotes &amp; Pools</a>; this page is about defining a region, assigning cells to it, and how data crosses the boundary.</p><div class="tip custom-block"><p class="custom-block-title">New and evolving (v1)</p><p>Regions are a recent addition and still stabilizing. The single-worker path — running a <em>whole</em> notebook <a href="/KaimonSlate.jl/previews/PR9/remotes#run-a-notebook-on-a-remote">on one host</a> — is the settled option; per-cell regions add power with the <a href="/KaimonSlate.jl/previews/PR9/regions#current-limits">caveats below</a>.</p></div><h2 id="Assigning-a-cell-to-a-region" tabindex="-1">Assigning a cell to a region <a class="header-anchor" href="#Assigning-a-cell-to-a-region" aria-label="Permalink to &quot;Assigning a cell to a region {#Assigning-a-cell-to-a-region}&quot;">​</a></h2><p>A cell&#39;s region is a <strong>cell tag</strong>, so it travels in the <code>.jl</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Tag</th><th style="text-align:right;">Runs on</th></tr></thead><tbody><tr><td style="text-align:right;"><em>(none)</em></td><td style="text-align:right;">the main kernel — local</td></tr><tr><td style="text-align:right;"><code>region=&lt;name&gt;</code></td><td style="text-align:right;">the named region</td></tr></tbody></table><p>You rarely type the tag by hand — three UI paths set it:</p><ul><li><p><strong>The 🏷 tag editor&#39;s &quot;Run on&quot; section</strong> — radio rows: <code>💻 local</code> (main kernel, this machine) and one <code>🖧 &lt;region&gt;</code> per declared destination, plus <strong>＋ Add destination…</strong>.</p></li><li><p><strong>The DAG node card&#39;s &quot;Run on&quot; picker</strong> — a dropdown on any node in the <a href="./dag">DAG pane</a>.</p></li><li><p><strong>Drag a node into a region zone</strong> — when regions exist, the DAG splits into columns (local + one per region); drop a cell into a column to run it there.</p></li></ul><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/tag-runon.png" alt=""></p><h2 id="Defining-a-region" tabindex="-1">Defining a region <a class="header-anchor" href="#Defining-a-region" aria-label="Permalink to &quot;Defining a region {#Defining-a-region}&quot;">​</a></h2><p>A <strong>region</strong> is a global, named compute definition — it lives in the <strong>🖧 Remotes → Regions</strong> manager on the front page, not in any one notebook. A region carries:</p><ul><li><p>a <strong>host</strong> (an SSH host) reached over a <strong>transport</strong> (<code>tunnel</code> or <code>direct</code>),</p></li><li><p>an optional <strong>preload</strong> — a <em>local</em> project dir whose environment is replicated on the host and precompiled on idle workers, so a notebook adopts a ready worker instead of cold-booting,</p></li><li><p>a <strong>data root</strong> — a <em>remote</em> path pinned as the workers&#39; <code>datadir()</code> / <code>@sfile</code>,</p></li><li><p>a <strong>cache root</strong> — a <em>remote</em> path for the region&#39;s own durable <a href="./memoization">cache</a>, kept separate from other workers on the box (so a co-located region moves a blob across the boundary rather than deduping it to nothing against a shared store),</p></li><li><p>a <strong>warm</strong> count — how many workers to keep booted and idle, ready to <em>adopt</em> (0 = cold spin on demand),</p></li><li><p>and worker <strong>thread counts</strong> (<code>&quot;&lt;compute&gt;,&lt;interactive&gt;&quot;</code>).</p></li></ul><p>Many regions can point at the <strong>same host</strong> with different config (e.g. <code>gpu</code> and <code>gpu_scratch</code> on one box with different data roots). Names are folded to identifiers (<code>slate-remote</code> → <code>slate_remote</code>) so they always match a <code>region=</code> tag.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/region-focus.png" alt=""></p><div class="tip custom-block"><p class="custom-block-title">Advanced, still settling</p><p>Two newer per-region knobs: an opt-in <strong>sysimage</strong> (a PackageCompiler image baked for the region&#39;s workers, for faster startup) and a <strong><code>curve</code></strong> toggle — the region&#39;s data channel is CURVE-encrypted by default; turn it off only for a co-located / loopback region where encryption is pure overhead.</p></div><h2 id="Using-a-region-in-a-notebook" tabindex="-1">Using a region in a notebook <a class="header-anchor" href="#Using-a-region-in-a-notebook" aria-label="Permalink to &quot;Using a region in a notebook {#Using-a-region-in-a-notebook}&quot;">​</a></h2><p>Open the <strong>Destinations</strong> manager (＋ Add destination… in the tag editor) and <strong>enable</strong> the regions this notebook uses — you pick from the regions already defined on the front page. This writes their names into the notebook&#39;s <strong><code>regions</code></strong> config footer (so the choice travels with the file); cells then tag <code>region=&lt;name&gt;</code>. Clearing all destinations brings the whole notebook back to the local kernel.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/destinations.png" alt=""></p><div class="tip custom-block"><p class="custom-block-title">Keep a region warm</p><p>A region with <strong>warm &gt; 0</strong> keeps that many workers booted and idle; running a tagged cell then <strong>adopts</strong> a ready worker (~1 s) instead of cold-booting (~90 s). Set the region&#39;s <code>preload</code> to the notebook&#39;s project so the adopted worker already has its packages loaded.</p></div><h2 id="How-boundary-values-cross" tabindex="-1">How boundary values cross <a class="header-anchor" href="#How-boundary-values-cross" aria-label="Permalink to &quot;How boundary values cross {#How-boundary-values-cross}&quot;">​</a></h2><p>When a cell runs, any value it <strong>reads</strong> that was produced on a <em>different</em> kernel is shipped to the cell&#39;s kernel first, <strong>just in time</strong> — you never move data by hand.</p><ul><li><p><strong>Only the boundary crosses.</strong> A large frame that&#39;s produced <em>and</em> consumed on the same region never leaves it; only a value read across a side boundary transfers.</p></li><li><p><strong>Content-addressed transfer.</strong> Values cross as content-addressed blobs over the dedicated data channel; a <code>DataFrame</code> crosses as Arrow IPC (see <a href="/KaimonSlate.jl/previews/PR9/memoization#arrow-tables-and-codecs">Memoization → Arrow</a>). Identical content dedups at every hop, and an unchanged value doesn&#39;t re-ship — a per-transfer freshness token (source run + any mutators + the current <code>@bind</code> value + the destination worker&#39;s generation) collapses a repeat to nothing.</p></li><li><p><strong>Progress is visible.</strong> A transfer past a size threshold drives the cell&#39;s own progress bar (<code>⇄ &lt;name&gt;: N/M MB ← host</code>), so a big move never looks like a hang.</p></li><li><p><strong>Live handles aren&#39;t shipped.</strong> A cell that produces a live DB connection / socket / open file (a <a href="./cell-tags"><code>resource</code></a> cell) can&#39;t cross the wire — instead its setup is <strong>replayed</strong> on the destination so each side opens its own handle. If a live handle would cross, the run stops and names the fix: tag the producing cell <code>resource</code>.</p></li></ul><p>Where the values actually flow, how fast, and over which link is all visible from the <a href="/KaimonSlate.jl/previews/PR9/dag#steering-regions-from-the-dag">DAG pane</a> — its <strong>⇄ peer routing plan</strong>, <strong>📊 transfers</strong> dashboard, and the <strong>mesh-connect</strong> consent live there. See <a href="/KaimonSlate.jl/previews/PR9/dag#steering-regions-from-the-dag">The Dependency Graph → Steering regions from the DAG</a>.</p><h2 id="Seeing-where-cells-run" tabindex="-1">Seeing where cells run <a class="header-anchor" href="#Seeing-where-cells-run" aria-label="Permalink to &quot;Seeing where cells run {#Seeing-where-cells-run}&quot;">​</a></h2><p>Every cell records <strong>where it last ran</strong> — surfaced across the <a href="./dag">DAG pane</a>:</p><ul><li><p><strong>🖧 badge + chips</strong> — a node whose last run was remote wears a <code>🖧</code> prefix; its details card shows a <em>last ran</em> chip (<code>⌂ local</code> / <code>🖧 &lt;host&gt;</code>) and a <em>moved ⇄ N MB</em> chip when a boundary transfer fed it.</p></li><li><p><strong>Region zones</strong> — the DAG lays out side-by-side columns (<code>💻 local · main kernel</code>, <code>🖧 &lt;name&gt; · &lt;host&gt;</code>); cross-column edges <em>are</em> the boundary transfers, and you drag cells between columns to reassign.</p></li><li><p><strong>🖧 region map</strong> — the DAG&#39;s region-map toggle recolors nodes by where they run, with a legend naming the hosts.</p></li></ul><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/dag-region-map.png" alt=""></p><p>Provenance only appears while a region is active — a plain local notebook shows none of it. For the worker-level view (roster, telemetry, where a notebook&#39;s kernels live), see <a href="/KaimonSlate.jl/previews/PR9/remotes#diagnostics">Remotes &amp; Pools → Diagnostics</a>.</p><h2 id="Restarting-a-region" tabindex="-1">Restarting a region <a class="header-anchor" href="#Restarting-a-region" aria-label="Permalink to &quot;Restarting a region {#Restarting-a-region}&quot;">​</a></h2><p>Restart a single region from its worker controls — it kills that region&#39;s worker, marks its cells stale, and re-runs them, leaving the rest of the notebook untouched. Closing the notebook or restarting the main worker tears down region kernels too.</p><h2 id="Current-limits" tabindex="-1">Current limits <a class="header-anchor" href="#Current-limits" aria-label="Permalink to &quot;Current limits {#Current-limits}&quot;">​</a></h2><p>Regions are powerful but still settling. Today:</p><ul><li><p><strong>Keep the main kernel local</strong> while a region is active.</p></li><li><p><strong><code>@bind</code>-declaring cells stay on the main kernel</strong> — declare controls locally, read them anywhere.</p></li><li><p><strong>Cross-boundary mutation is undefined.</strong> A region cell should <em>produce</em> values, not mutate an object that lives on the main kernel. An untagged cell that mutates a region value auto-follows its target one hop, but deeper mutation chains aren&#39;t guaranteed.</p></li></ul><p>Expect this surface to grow; the whole-notebook <a href="/KaimonSlate.jl/previews/PR9/remotes#run-a-notebook-on-a-remote">remote placement</a> is the conservative choice when you want everything elsewhere.</p><h2 id="From-the-agent" tabindex="-1">From the agent <a class="header-anchor" href="#From-the-agent" aria-label="Permalink to &quot;From the agent {#From-the-agent}&quot;">​</a></h2><p>Under <a href="./agent">Kaimon</a>, define a region with <code>slate_region(name; host, warm, preload, data_root, …)</code>, choose which regions a notebook uses with <code>slate_region_on(notebook, &quot;name1,name2&quot;)</code>, and list the registry with <code>slate_regions()</code>.</p><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><p><a href="./remotes">Remotes &amp; Pools</a> — hosts, transports, warm pools, <code>sync_memo</code>, data-transfer settings.</p></li><li><p><a href="./dag">The Dependency Graph</a> — the DAG pane, zones, and the region map.</p></li><li><p><a href="./memoization">Memoization &amp; Caching</a> — the content-addressed store the transfers ride on.</p></li><li><p><a href="./cell-tags">Cell Tags &amp; Caching</a> — the <code>region=</code> and <code>resource</code> tags.</p></li></ul>',39)])])}const m=o(r,[["render",s]]);export{p as __pageData,m as default};
