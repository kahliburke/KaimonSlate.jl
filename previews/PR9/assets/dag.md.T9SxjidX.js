import{_ as a,o as t,c as o,a2 as s}from"./chunks/framework.BtKjyiyt.js";const p=JSON.parse('{"title":"The Dependency Graph","description":"","frontmatter":{},"headers":[],"relativePath":"dag.md","filePath":"dag.md","lastUpdated":null}'),r={name:"dag.md"};function n(l,e,i,h,d,g){return t(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="The-Dependency-Graph" tabindex="-1">The Dependency Graph <a class="header-anchor" href="#The-Dependency-Graph" aria-label="Permalink to &quot;The Dependency Graph {#The-Dependency-Graph}&quot;">​</a></h1><p>Every notebook is a <strong>directed acyclic graph</strong> of cells: the engine reads which variables each cell reads and writes, and draws an edge wherever one cell consumes another&#39;s value. That graph is what makes the notebook <a href="./reactivity">reactive</a> — change a value and only the downstream cells restale.</p><p>This page is about <strong>seeing and shaping</strong> that graph: the DAG pane, manual edges for effects no variable carries, and running part of a notebook on another kernel (regions).</p><h2 id="The-DAG-pane" tabindex="-1">The DAG pane <a class="header-anchor" href="#The-DAG-pane" aria-label="Permalink to &quot;The DAG pane {#The-DAG-pane}&quot;">​</a></h2><p>Open the pane with the <strong>🕸 DAG</strong> button in the top bar, or <strong>⌘⇧G</strong>. It&#39;s a live map of the notebook&#39;s dataflow — one node per cell, an edge wherever a cell reads a value another cell writes — laid out automatically and updated as you edit and run.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/dag-pane.png" alt=""></p><ul><li><p><strong>Node color</strong> mirrors each cell&#39;s <a href="/KaimonSlate.jl/previews/PR9/reactivity#cell-states">state</a> — fresh / stale / running / errored — so a wave of gold shows exactly what a change restaled.</p></li><li><p><strong>⚙ display</strong> — <strong>show setup cells</strong> (<code>using</code>/<code>import</code> and their edges) and <strong>show isolated cells</strong> (cells with no dataflow edges) are off by default, keeping the graph to the cells that actually pass data.</p></li><li><p><strong>⇅ direction</strong> — layout orientation; <em>auto</em> follows the pane shape (a tall pane lays out top-down).</p></li><li><p><strong>◨ dock</strong> and the <strong>grip</strong> re-side and resize the pane.</p></li><li><p><strong>🔥 heat map</strong> — color cells by accumulated compute time (hotter = more expensive), to find the bottleneck in a pipeline.</p></li><li><p><strong>🖧 region map</strong> — color cells by <em>where they run</em> (see <a href="/KaimonSlate.jl/previews/PR9/dag#regions-run-cells-on-another-kernel">Regions</a>).</p></li></ul><h2 id="Manual-edges" tabindex="-1">Manual edges <a class="header-anchor" href="#Manual-edges" aria-label="Permalink to &quot;Manual edges {#Manual-edges}&quot;">​</a></h2><p>The engine infers edges from the variables a cell reads and writes. Some dependencies aren&#39;t carried by any variable, though — a cell that writes a file, seeds a database table, or mutates global state that a later cell reads back. Assert those yourself so the engine keeps the two in order:</p><ul><li><p><strong><code>needs=id1,id2</code></strong> header tag — set it with the <strong>🏷 tag editor</strong> or directly in the cell header. It names the earlier code cells this one depends on.</p></li><li><p><strong>🔗 link mode</strong> in the DAG pane — click it, then click two cells to draw a manual edge (shown <strong>dashed</strong>); click a dashed edge to remove it.</p></li></ul><p>Manual edges are first-class: they drive staleness, run order, and cache keys exactly like inferred ones. A <code>needs=</code> that names no earlier code cell (deleted, moved below, or a markdown cell) is inert and flagged with a <strong>🔗⚠</strong> badge on the cell.</p><h2 id="Regions-—-run-cells-on-another-kernel" tabindex="-1">Regions — run cells on another kernel <a class="header-anchor" href="#Regions-—-run-cells-on-another-kernel" aria-label="Permalink to &quot;Regions — run cells on another kernel {#Regions-—-run-cells-on-another-kernel}&quot;">​</a></h2><p>When a notebook is split into <strong>regions</strong>, some cells run on a second kernel (usually on another host) while the rest stay local. The DAG pane is where you <strong>see and steer</strong> that split; the region model itself — defining regions, declaring destinations, how boundary values cross — is in <strong><a href="./regions">Regions</a></strong>.</p><h2 id="Steering-regions-from-the-DAG" tabindex="-1">Steering regions from the DAG <a class="header-anchor" href="#Steering-regions-from-the-DAG" aria-label="Permalink to &quot;Steering regions from the DAG {#Steering-regions-from-the-DAG}&quot;">​</a></h2><p>Turn on the <strong>🖧 region map</strong> and the graph reorganizes into <strong>side-by-side zones</strong> — <code>💻 local</code> and one per region — with each node tinted by where it runs and the cross-zone edges drawn as the boundary hand-offs (labeled by transport: <em>direct</em> / <em>ssh-bridge</em> / <em>relay</em>). <strong>Drag a node between zones</strong> to reassign it. A node wears a <strong>🖧</strong> badge when its last run executed on a region kernel, and the legend along the bottom names the hosts and hosts the region toolbar.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/dag-region-map.png" alt=""></p><p><strong>Click any node</strong> for its detail card. For a cell whose last run was remote it shows the <strong>provenance</strong>: a <em>last ran</em> <code>🖧 host</code> chip, a <em>moved</em> <code>⇄ N MB</code> chip for the inputs that had to cross the boundary, a per-transfer breakdown (size · route · throughput), and a <strong>Run on</strong> picker to reassign the cell to a different kernel in place.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/dag-node-card.png" alt=""></p><p>Each region <strong>zone header</strong> carries a live status dot — hover it for the region&#39;s worker card: connection status, host, transport, and CPU / RSS / running-cell telemetry, with <strong>🪵 Log</strong> and <strong>✕ Reap</strong> actions.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/region-hover-card.png" alt=""></p><h3 id="The-region-toolbar" tabindex="-1">The region toolbar <a class="header-anchor" href="#The-region-toolbar" aria-label="Permalink to &quot;The region toolbar {#The-region-toolbar}&quot;">​</a></h3><p>The region-map legend hosts three tools for the cross-region plumbing.</p><p><strong>⇄ peer routing plan</strong> — how each region pair is wired: a resolved <strong>direct</strong> or <strong>ssh-bridge</strong> route worker-to-worker, or a <strong>relay</strong> through the hub, with per-host grants and host-key pins. <strong>↻ recalculate</strong> probes every pair live so throughput and route are measured <em>now</em> rather than on the next real transfer.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/dag-peer-plan.png" alt=""></p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/dag-peer-plan-probing.png" alt=""></p><p><strong>📊 transfers</strong> — a dashboard of every boundary move: total moved, throughput over time, a region-to-region grid, and the rate distribution — to see where the data actually flows and how fast.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/dag-transfers.png" alt=""></p><p><strong>⇄ connect</strong> — the first time two regions on different hosts need a direct worker-to-worker link, Slate asks before exchanging keys (decline and transfers still work — they just relay through the hub). The dialog spells out exactly what it installs — an on-host <strong>ed25519 key</strong>, a locked-down <strong>single-port grant</strong>, a <strong>host-key pin</strong> — then arms the bridge with live progress.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/mesh-consent.png" alt=""></p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/mesh-connecting.png" alt=""></p><p>While a value crosses the boundary, the <strong>consuming cell</strong> shows a live progress bar (<code>⇄ &lt;name&gt;: N/M MB ← host</code>) — driven on its own data channel, so a big move never looks like a hang.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/cell-transfer-progress.png" alt=""></p><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><p><a href="./reactivity">Reactive Cells</a> — the reactive model the graph drives, and the 🔗 upstream focus.</p></li><li><p><a href="./regions">Regions</a> — running part of a notebook on a second kernel, steered from the DAG.</p></li><li><p><a href="./remotes">Remotes &amp; Pools</a> — hosts, transports, and warm pools.</p></li><li><p><a href="./cell-tags">Cell Tags &amp; Caching</a> — the <code>needs=</code> and <code>region=</code> tags in the header.</p></li></ul>',34)])])}const u=a(r,[["render",n]]);export{p as __pageData,u as default};
