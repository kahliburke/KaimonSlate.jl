import{_ as t,o,c as a,a2 as i}from"./chunks/framework.XxxS7AoL.js";const g=JSON.parse('{"title":"Project Files","description":"","frontmatter":{},"headers":[],"relativePath":"project-files.md","filePath":"project-files.md","lastUpdated":null}'),s={name:"project-files.md"};function r(n,e,d,h,l,c){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="Project-Files" tabindex="-1">Project Files <a class="header-anchor" href="#Project-Files" aria-label="Permalink to &quot;Project Files {#Project-Files}&quot;">​</a></h1><p>A notebook usually sits inside a Julia project, and most of the work around it is in that project&#39;s other files: the package source your cells call, the data they read, the JavaScript a web cell loads. The <strong>📁 Files</strong> panel puts those in the browser beside the notebook.</p><p>Open it from the <strong>📁 Files</strong> button in the top bar, or by clicking the notebook&#39;s title. There is no keyboard shortcut.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/files-panel.png" alt=""></p><h2 id="What-the-tree-shows" tabindex="-1">What the tree shows <a class="header-anchor" href="#What-the-tree-shows" aria-label="Permalink to &quot;What the tree shows {#What-the-tree-shows}&quot;">​</a></h2><p>The root is the <strong>enclosing project</strong>: the directory holding the nearest <code>Project.toml</code>, found by walking up from the notebook&#39;s own file. A notebook at <code>myproj/notebooks/analysis.jl</code> gets a tree rooted at <code>myproj/</code>, with <code>notebooks/</code> as one folder inside it.</p><p>That is the same root <code>@asset</code> and <code>datadir()</code> resolve against, so what you see here is what those reach.</p><h3 id="The-data-directory" tabindex="-1">The data directory <a class="header-anchor" href="#The-data-directory" aria-label="Permalink to &quot;The data directory {#The-data-directory}&quot;">​</a></h3><p><code>datadir()</code> returns the notebook&#39;s own data directory, and <code>@sfile &quot;flights.csv&quot;</code> is a path into it that you can read from and write to. Using them instead of a hardcoded path keeps the notebook portable between machines.</p><p>The directory ignores itself in git by construction, so a stray database is never committed. It is where a <a href="./widgets"><code>FileUpload</code></a> puts a reader&#39;s file, it travels with an exported <a href="./app-mode">app</a>, and a <a href="./regions">region</a> can repoint it with its own data root.</p><p>A notebook with no enclosing project has no source to browse, and the panel says so.</p><p>Some things are never listed: <code>.git</code>, <code>.julia</code>, <code>node_modules</code>, <code>compiled</code>, <code>build</code>, <code>.cache</code>, <code>__pycache__</code>, <code>.vscode</code>, <code>.claude</code>, <code>.ipynb_checkpoints</code>, and <code>.DS_Store</code> / <code>Thumbs.db</code> / <code>.gitkeep</code>. Dotfiles are hidden until you press the <code>·</code> button, which remembers your choice per notebook. The tree does not read <code>.gitignore</code>, so an ignored file that is not on that list still appears.</p><p>Two conveniences above the tree: a filter box that matches loosely against the whole path, so <code>srvcmp</code> finds <code>src/server_complete.jl</code>, and a strip listing every notebook the hub is currently serving, with error counts, for jumping between them.</p><p><code>.jl</code> files that look like notebooks are marked with 📓 and can be opened as notebooks from the right-click menu.</p><h2 id="Reading-a-file" tabindex="-1">Reading a file <a class="header-anchor" href="#Reading-a-file" aria-label="Permalink to &quot;Reading a file {#Reading-a-file}&quot;">​</a></h2><p>Click a file to open it on the right.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Kind</th><th style="text-align:right;">What you get</th></tr></thead><tbody><tr><td style="text-align:right;">Text</td><td style="text-align:right;">A full editor with line numbers, find and replace (⌘F), bracket matching, ⌘/ to comment, and Julia autocompletion</td></tr><tr><td style="text-align:right;">Image</td><td style="text-align:right;">A preview with its pixel dimensions</td></tr><tr><td style="text-align:right;">Audio, video</td><td style="text-align:right;">A player</td></tr><tr><td style="text-align:right;">Anything else</td><td style="text-align:right;">&quot;Binary file, not shown&quot;, with a download link</td></tr></tbody></table><p>Classification is by filename. Around 60 extensions are treated as text, plus common extensionless names like <code>Dockerfile</code>, <code>Makefile</code>, <code>LICENSE</code> and <code>README</code>. A binary file and an <code>.svg</code> both offer <strong>≡ Open as text</strong> if you want to see the bytes anyway.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/files-editor.png" alt=""></p><p>Syntax highlighting covers the four grammars the editor bundles: CSS, HTML, JavaScript and Julia. Julia&#39;s grammar is reused for Python, R, TOML, YAML and shell, which share its comment character and read acceptably. C, Rust, Go, Lua, SQL and TeX open without highlighting.</p><p>Files over 4 MB will not open in the editor, and a file that is not valid UTF-8 is refused rather than mangled.</p><p>Your cursor and scroll position in each file are remembered while the notebook is open.</p><h2 id="Editing-and-saving" tabindex="-1">Editing and saving <a class="header-anchor" href="#Editing-and-saving" aria-label="Permalink to &quot;Editing and saving {#Editing-and-saving}&quot;">​</a></h2><p>Save with ⌘S. There is no confirmation.</p><p>Slate records the modification time when you open a file and sends it back when you save. If the file changed on disk in between, the save is refused and you get a choice of <strong>Reload from disk</strong>, <strong>Overwrite</strong>, or cancel. Switching to another file with unsaved changes offers to save first, and so does closing the browser tab.</p><p>While the panel is open, Slate re-checks the open file whenever the window regains focus. A clean buffer is quietly replaced with the new contents. A dirty one gets a <strong>changed on disk</strong> badge and waits for you to decide at save time.</p><p><strong>New file</strong> and <strong>New folder</strong> are in the right-click menu. A new folder stays invisible in the tree until it has something in it.</p><h3 id="What-a-save-does" tabindex="-1">What a save does <a class="header-anchor" href="#What-a-save-does" aria-label="Permalink to &quot;What a save does {#What-a-save-does}&quot;">​</a></h3><p>Saving writes to disk. Nothing re-runs on its own.</p><p>If the file is package source that the notebook&#39;s worker has loaded, Revise picks it up, and Slate marks stale every cell that reads a name the edit changed, plus everything downstream of those. A banner tells you how many cells are affected. You choose when to run them. If your edit does not parse, the banner says that instead.</p><p>This is on by default and can be turned off per notebook. See <a href="./hot-reload">Editing project source</a>, which also covers the edits it will not catch.</p><p>Saving a file a cell reads through <code>@asset</code> is different: those cells recompute immediately, because that is what <code>@asset</code> is for.</p><div class="warning custom-block"><p class="custom-block-title">Editing the notebook&#39;s own file</p><p>The tree marks the notebook&#39;s own <code>.jl</code> and will not let you rename or delete it, but it will let you open and save it. Doing so writes the file underneath the running notebook, which then re-parses it. Use the notebook itself to edit its cells.</p></div><h2 id="rearranging" tabindex="-1">Rearranging <a class="header-anchor" href="#rearranging" aria-label="Permalink to &quot;Rearranging&quot;">​</a></h2><p>Right-click any row for the menu, or use the tree&#39;s keyboard shortcuts: arrows to move and expand, Enter to open, <strong>F2</strong> to rename, <strong>⌫</strong> to delete, <code>/</code> to jump to the filter.</p><ul><li><p><strong>Rename</strong> also moves. Type a name to rename in place, or a path with <code>/</code> in it to move within the project. It never overwrites an existing file.</p></li><li><p><strong>Duplicate</strong> works on files only, auto-naming <code>thing-copy.jl</code>, then <code>-copy2</code>, and so on.</p></li><li><p><strong>Delete</strong> asks first, counting the contents for a folder. Deleting a folder needs you to confirm the recursive delete, and is refused if the notebook&#39;s own file is inside it.</p></li><li><p><strong>New folder</strong> creates intermediate directories as needed.</p></li><li><p><strong>Copy path</strong> puts the absolute path on your clipboard.</p></li></ul><p>Deletes are permanent. There is no trash, and the notebook&#39;s own undo history does not cover them.</p><h2 id="Adding-files" tabindex="-1">Adding files <a class="header-anchor" href="#Adding-files" aria-label="Permalink to &quot;Adding files {#Adding-files}&quot;">​</a></h2><p>Drag files from your desktop onto a folder row to put them there, or onto empty space in the tree to put them in the project root. Dropping without a target folder lands them in <code>assets/</code>.</p><p>Names are sanitised, an upload identical to a file already there is reused rather than duplicated, and a name clash with different contents is uniquified. The limit is 64 MB per file.</p><h2 id="With-a-remote-worker" tabindex="-1">With a remote worker <a class="header-anchor" href="#With-a-remote-worker" aria-label="Permalink to &quot;With a remote worker {#With-a-remote-worker}&quot;">​</a></h2><p>The tree is always your own machine&#39;s filesystem. There is no remote browser.</p><p>For package source this works out, because Slate rsyncs the project&#39;s <code>src/</code> (and any dev&#39;d path dependencies) to the remote as it changes, so a save hot-reloads on the remote worker the same way it does locally.</p><p>Files outside <code>src/</code> are not carried by that. Data files reach a remote worker through a separate mechanism that syncs the notebook&#39;s data directory at run time. A <code>.js</code> or <code>.csv</code> sitting elsewhere in the project has no sync path at all.</p><h2 id="access" tabindex="-1">Access <a class="header-anchor" href="#access" aria-label="Permalink to &quot;Access&quot;">​</a></h2><p>Every path is confined to the project root. A path that tries to escape, by any spelling, is rejected before anything is read or written.</p><p>That confinement is the only restriction. There is <strong>no authentication</strong>, so anything that can reach the hub&#39;s port can read, write and delete anywhere in the project directory. The hub binds <code>127.0.0.1</code> by default, which is what makes this reasonable. Binding it to <code>0.0.0.0</code> exposes your project&#39;s files to the whole network. There is a Host and Origin check that stops a hostile web page in your own browser from reaching the hub, but it does not stop a direct request.</p><p><a href="./app-mode">App mode</a> does not serve any of these routes, so a visitor to an app or a <a href="/KaimonSlate.jl/previews/PR32/app-mode#Workbook-mode">workbook</a> has no file access at all.</p><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><p><a href="./notebook-basics">Notebook Basics</a> for the notebook itself</p></li><li><p><a href="./live-updates">Live Updates</a> for <code>@asset</code> and cells that react to a file</p></li><li><p><a href="./frontend-extensions">Front-end Extensions</a> for keeping web-cell JavaScript and CSS in real files</p></li><li><p><a href="./hot-reload">Editing project source</a> for what happens after you save package source</p></li><li><p><a href="./packages">Packages</a> for the project&#39;s environment</p></li></ul>',50)])])}const u=t(s,[["render",r]]);export{g as __pageData,u as default};
