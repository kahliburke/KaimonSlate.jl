import{_ as a,o as t,c as o,a2 as s}from"./chunks/framework.XxxS7AoL.js";const u=JSON.parse('{"title":"Editing project source","description":"","frontmatter":{},"headers":[],"relativePath":"hot-reload.md","filePath":"hot-reload.md","lastUpdated":null}'),n={name:"hot-reload.md"};function r(i,e,c,l,h,d){return t(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="Editing-project-source" tabindex="-1">Editing project source <a class="header-anchor" href="#Editing-project-source" aria-label="Permalink to &quot;Editing project source {#Editing-project-source}&quot;">​</a></h1><p>A notebook that lives inside a package spends most of its time calling that package&#39;s functions. When you change one, Slate notices, reloads the code into the running worker, and marks the cells that looked like they depended on it. You decide when to run them.</p><p>Nothing runs on its own. The result of a source edit is a banner and a set of stale cells.</p><h2 id="What-you-see" tabindex="-1">What you see <a class="header-anchor" href="#What-you-see" aria-label="Permalink to &quot;What you see {#What-you-see}&quot;">​</a></h2><p>A banner appears at the top left:</p><blockquote><p>🔁 <strong>Project source changed</strong> — ~4 cells likely affected (our guess may be incomplete).</p></blockquote><p>with <strong>Run affected</strong>, <strong>Re-run all (safe)</strong> and a dismiss button. It stays until you act on it.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/srcreload-banner.png" alt=""></p><p>When Slate cannot work out which cells are involved it says so and leads with <strong>Re-run all (safe)</strong> instead. If your edit does not compile you get a red banner naming the error, with no buttons, and tracking resumes as soon as the file parses again.</p><p><img class="slate-shot" src="https://github.com/kahliburke/KaimonSlate.jl/releases/download/docs-assets/srcerror-banner.png" alt=""></p><p>Ignoring the banner is fine. The affected cells are already stale, so the ordinary Run-stale path picks them up whenever you get to it.</p><h2 id="What-counts-as-a-change" tabindex="-1">What counts as a change <a class="header-anchor" href="#What-counts-as-a-change" aria-label="Permalink to &quot;What counts as a change {#What-counts-as-a-change}&quot;">​</a></h2><p>Slate parses each tracked file and hashes every top-level definition in it: functions, structs, abstract and primitive types, macros, and <code>const</code>s. A definition counts as changed when it is added, removed, or its body hashes differently. Moving code around a file without changing it is not a change.</p><p>If a package&#39;s <code>__init__</code> is among them, Slate re-runs it for that package. Revise does not do that on its own.</p><h2 id="How-cells-get-chosen" tabindex="-1">How cells get chosen <a class="header-anchor" href="#How-cells-get-chosen" aria-label="Permalink to &quot;How cells get chosen {#How-cells-get-chosen}&quot;">​</a></h2><p>The changed names are matched against what each cell reads, including dotted paths, so an edit to <code>greet</code> reaches a cell reading <code>MyPkg.Sub.greet</code>. Everything downstream of a match goes stale too.</p><p>There is also a deliberately coarse rule: if a changed name is one a cell provides, every name that cell provides joins the set. In practice the cell in question is your <code>using MyPkg</code> cell, so one changed export marks every cell that uses the package. Those cells would recompute on a re-run anyway, because any source edit changes the project&#39;s source digest.</p><p>There is no call-graph analysis. The matching is by name against the notebook&#39;s existing dependency graph.</p><h3 id="What-it-will-miss" tabindex="-1">What it will miss <a class="header-anchor" href="#What-it-will-miss" aria-label="Permalink to &quot;What it will miss {#What-it-will-miss}&quot;">​</a></h3><p>Cases that produce no banner:</p><ul><li><p><strong>Definitions generated by a macro or a loop.</strong> <code>@enum</code>, or <code>@eval</code> inside a <code>for</code>, produce names the parser never sees. <code>Base.@kwdef</code> is fine.</p></li><li><p><strong>A cell that reaches the code indirectly</strong>, through a function that calls the edited one. It is caught only by the package-wide rule above, which needs the <code>using</code> cell to have run successfully at least once.</p></li><li><p><strong><code>locked</code> cells</strong>, which are never restaled by anything. See <a href="./cell-tags">Cell Tags</a>.</p></li><li><p><strong>Files Revise does not track</strong>, such as a <code>.jl</code> a cell <code>include</code>s directly rather than one that is part of a package module.</p></li></ul><p>And the limits that are Revise&#39;s rather than Slate&#39;s: reassigning a <code>const</code>, or changing a struct whose instances already exist, needs a worker restart. Slate does not work around those.</p><h2 id="caching" tabindex="-1">Caching <a class="header-anchor" href="#caching" aria-label="Permalink to &quot;Caching&quot;">​</a></h2><p>A source edit changes the cache key of every cell, so nothing computed under the old code is restored. The old entries are not deleted, they simply stop matching, and re-storing is cheap because the data blobs are shared.</p><p>This happens in the worker and is not affected by the toggle below. Turning hot-reload off stops the banner and the automatic staling; it does not make Slate serve results from code you have changed.</p><p>The digest covers the enclosing project&#39;s <code>src/</code> and the <code>src/</code> of any path dependency the environment develops. A tracked package outside those directories can produce a banner without changing any cache key.</p><h2 id="Turning-it-off" tabindex="-1">Turning it off <a class="header-anchor" href="#Turning-it-off" aria-label="Permalink to &quot;Turning it off {#Turning-it-off}&quot;">​</a></h2><p><strong>☰ → ⚙ Settings → This notebook → Execution → Hot-reload /src edits</strong>. It takes effect immediately, and persists in the notebook&#39;s config footer.</p><p>There is nothing to set up otherwise. It is on whenever the notebook runs on a worker (not an in-process kernel) and sits inside a project. The <a href="./project-files">Files panel</a> is the intended way to edit that source without leaving the browser, but an external editor works identically.</p><h2 id="Remote-workers-and-regions" tabindex="-1">Remote workers and regions <a class="header-anchor" href="#Remote-workers-and-regions" aria-label="Permalink to &quot;Remote workers and regions {#Remote-workers-and-regions}&quot;">​</a></h2><p>Hot-reload works over a remote worker. Slate watches the local project&#39;s <code>src/</code>, and the <code>src/</code> of each developed path dependency, and rsyncs changes to the remote as you save.</p><div class="tip custom-block"><p class="custom-block-title">Revise has to be in your own global environment</p><p>Slate installs Revise into a remote worker only if you have it in your own global <code>v1.x</code> environment, on the principle of mirroring your setup rather than imposing one. If you do not use Revise locally, a remote worker or a region gets no hot-reload, and nothing says so.</p></div><p>Files outside <code>src/</code> are not carried by that sync. See <a href="/KaimonSlate.jl/previews/PR32/project-files#With-a-remote-worker">Project Files</a>.</p><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><p><a href="./project-files">Project Files</a> for editing the source in the browser</p></li><li><p><a href="./packages">Packages</a> for the environment the project sits in</p></li><li><p><a href="./memoization">Memoization &amp; Caching</a> for what else invalidates a cached result</p></li><li><p><a href="./remotes">Remotes</a> and <a href="./regions">Regions</a></p></li></ul>',35)])])}const g=a(n,[["render",r]]);export{u as __pageData,g as default};
